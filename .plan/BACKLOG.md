# BACKLOG — zentala.eu

**Chapter ideas and unwritten subjects: [`vision/CHAPTER-IDEAS.md`](vision/CHAPTER-IDEAS.md)** — the
map of everything started, cut or only said. Add a row there whenever a subject appears.

Open ideas and future improvements not yet assigned to an epic.

---

## Content / Copy

- [ ] Add real expert quotes to `Testimonials` component (currently component exists but is unused on homepage — only in `/ui` dev library). Find actual EU reform advocates, analysts, thinkers.
- [ ] Home page: shorten benefits section
- [ ] Home page: split benefits into "for citizens", "for businesses", "for regions", "for European integration"
- [ ] Home page: add "Problems we address / EU Challenges" section (security, political unity, R&D, migration, energy, healthcare, retirement, education, economic power)
- [ ] `/why` page: connect transcripts, summarize as infographic with links to videos
- [ ] `/why` content in Polish → move to zentala.pl as intro
- [ ] Leave feedback / comments / likes / share mechanics — some dynamic element

## Structure / Navigation

- [ ] Reorganize all content around 3 pillars: Language Integration, Digital Integration, Robotic Reindustrialization
- [ ] Mobile navigation: expand menu, add transitions, better topic grouping
- [ ] Breadcrumbs navigation
- [ ] Fix all broken links (see `missing_links.md`)
- [ ] Sidebar navigation for content browsing

## Infrastructure / Integrations

- [ ] Configure Appwrite email templates for newsletter confirmation to look like a proper subscription confirmation (not default Appwrite emails)
  - URL: https://cloud.appwrite.io/console/project-eu/auth/templates

## Data & Integrations

- [ ] Replace hardcoded sample stats in `InteractiveSection.astro` with real Eurostat API data
  - Currently: hardcoded language %, DESI scores, fictional GDP gains
  - File: `src/components/InteractiveSection.astro` lines 27-53
  - Note at bottom: "Data is for illustrative purposes"
- [ ] Algolia: fix oversized records before full integration (3 transcripts exceed 10KB limit)
  - Files listed in `todo_algolia.md`
  - Run `npm run index-algolia` after fixing
  - `algoliaClient.ts` currently uses mock client — needs real init after env vars confirmed

## Visual / UX

- [ ] Improve card visuals: better icons and illustrations
- [ ] Improve readability of long lists (split into digestible sections)
- [ ] Subtle animations and transitions (smooth scroll, card entrances)
- [ ] Interactive timeline for reform implementation
- [ ] "Continue reading" / "related topics" at end of each page
- [ ] Table of contents for long articles

## Technical Debt (from TODO_CLEANUP.md)

- [ ] Fix TypeScript warnings in `EUStatisticsChart.astro` (Chart ID handling)
- [ ] Remove unused imports in `src/pages/index.astro` (ArrowRightIcon, RobotIcon, DigitalIcon, LanguageIcon, InteractiveSection, Testimonials)
- [ ] Remove unused var `isDevelopment` in `src/components/Footer.astro`
- [ ] Remove unused vars `borderColorClass`, `bgColorClass` in `WhyDigitalReform.astro`
- [ ] Remove unused var `benefitsContainer` in `src/pages/benefits.astro`
- [ ] Remove unused var `statusMessage` in `src/pages/confirm.astro`
- [ ] Remove unused var `isProduction` in `src/pages/ui/index.astro`
- [ ] Remove unused imports `Card`, `CardGrid` in vision components
- [ ] Fix deprecated `frameborder` → `border-0` class in all iframes
- [ ] Fix CardGrid usage in `content/docs/all.mdx`

## Performance

- [ ] Lazy loading for images and heavy elements
- [ ] Image and asset optimization
- [ ] Code splitting for faster initial load
- [ ] Optimize Algolia indexing: searchable attributes, facets, filters

## SEO & Metadata

- [ ] Optimize all page metadata
- [ ] Add structured data (JSON-LD)
- [ ] Create XML sitemap
- [ ] Enhance meta descriptions and titles

## Accessibility

- [ ] WCAG compliance audit
- [ ] Keyboard navigation improvements
- [ ] Cross-browser and device testing

- [ ] **No check for links pointing to draft pages** — 24 files have `draft: true` (`src/content/config.ts:10`), and drafts are dropped from the prod build (`src/pages/docs/[...slug].astro:10`), so a link to one works in dev and 404s in prod. `check-links.js:8`, `site-crawler.js:7`, `url-checker.js:7` crawl only a running dev server, where drafts are visible, and skip external links (`site-crawler.js:13`). Fix: one static audit script over `src/content` (links → draft/missing targets, external links via HEAD, pages with no incoming link) plus `just check`. (High, 5)

- [ ] **The repo has no installed dependencies, and nothing says so** — neither `zentala.eu/node_modules` nor a fresh worktree has them, and `yarn` is not on PATH (use `corepack yarn install`). Cost on 2026-09-19: four wasted build attempts, one killed by the OS (exit 137) before the content audit could run. Fix: say it in `CLAUDE.md` under Commands, and give the repo a `justfile` with `just setup` (`rules/just.md`). (Medium, 2)

## Top-level frame (2026-09-20)

Source: [`vision/aging-society-frame.md`](vision/aging-society-frame.md). Editorial
assessment of every draft: [`reports/2026-09-19-content-audit.md`](reports/2026-09-19-content-audit.md)
plus the draft review recorded in this session.

- [x] **Homepage leads with the pillars, not with the reason for them** — DONE 2026-09-21, commit d594a9a: frame section above the pillars on `src/pages/index.astro`. — a visitor meets three parallel boxes before any argument for why Europe must act at all. Put the demographic frame above them: aging society → the pension model breaks → we design the replacement now. Done for `/vision` (`src/components/vision/FrameIntro.astro`, commit a7145b7); still open for the homepage `src/pages/index.astro`. (High, 5)
- [x] **The three pillars are presented as peers; they are layers with an order** — done on `/vision` (`src/components/vision/LayerStack.astro`, commit a7145b7): numbered levels with a "makes possible" connector. Homepage `src/pages/index.astro` still shows three parallel cards — follow-up in the entry above. (High, 5)
- [ ] **"Who captures the gains" is not visible anywhere** — shown beside the stack on `/vision` (`src/components/vision/CrossCuttingNote.astro`, commit a7145b7). Still missing on the three detail pages `src/pages/vision/*-integration.astro`, `src/pages/vision/robotic-reindustrialization.astro`; the component is reusable, drop it in. (High, 2)
- [x] **Direct democracy, predictive healthcare and autonomous transport are listed as peers of the pillars** — regrouped under their layers on `/vision` (commit a7145b7). The old "Additional Focus Areas" (environment, local law, education, street naming) were dropped from the page; they have no page behind them, so nothing was lost. (Medium, 3)
- [x] **No chapter exists for the core of the vision: demography and pensions** — DONE: `src/content/docs/book/designing-our-retirement.md` (commit 5f2469b). — `book/principles.md` declares it as the heart of the argument and no file covers it. This is now the site's opening argument and has nothing behind it. (High, 5)
- [x] **No chapter on robotics/reindustrialisation** — DONE: `src/content/docs/book/robotic-reindustrialisation.md` (commit a849d11). — it is the ceiling of the stack and the sidecar names it as a pillar, yet `src/pages/vision/robotic-reindustrialization.astro` has no content file behind it. Include the military-R&D-to-civilian-conversion path. (High, 5)
- [ ] **Every page should leave the reader with something to carry** — analysis that informs and leaves the reader powerless fails the site's purpose. Each substantial piece ends with a named first step or an invitation. Needs an editorial checklist, not a one-off pass. (Medium, 3)

## Publishing the drafts (2026-09-20)

12 of 23 "drafts" are one-sentence stubs, not content — they are the holes behind
29 dead internal links. Verdicts from the editorial review:

- [x] **Publish today, minor fixes only (5)** — DONE 2026-09-20 (phase dates moved to 2027-2042). — `book/principles.md`, `book/european-egovernment.mdx`, `book/eGov-challenges.mdx` (add 2-3 examples), `ideas/governance/blockchain-state.mdx`, `ideas/transportation/autonomous-transportation.mdx` (phase dates 2025-2030 are stale). (High, 3)
- [ ] **Merge the eGovernment cluster** — `book/eGov-vison.mdx` duplicates `book/european-egovernment.mdx` by ~70% (and its filename is misspelled); fold in `eGov-challenges` as a section and `blockchain-state` as a component. One chapter: vision → components → challenges. (Medium, 5)
- [ ] **Merge the language cluster — five files, one reform** — `book/language.md` (a social post, not a chapter), `book/language-integration-administrative-implementation.mdx` (bare `<Card>`s, Polish comments), `book/economic-growth-language-unity.mdx` (one sentence), into the published `book/european-english.mdx` and `ideas/culture/language-integration.mdx`. (Medium, 5)
- [ ] **Merge the payments cluster into one hub** — `unified-payment-zone` + `near-zero-transaction-costs` + `digital-receipts` are three stubs of one topic; `ideas/finance/european-cryptocurrency.mdx` must address the digital euro (its Libra references are obsolete). (Medium, 5)
- [ ] **Delete the Starlight placeholder** — `docs/reference/example.md` plus the link to it in `reference/index.mdx:13`. (Low, 1)
- [ ] **Two empty transcripts** — `transcripts/when-europe-falters-america-gains.mdx` (body is `# todo`, but its keyPoints are good) and `transcripts/europe-should-take-over-ukrainian-sky.mdx`. Both cover US withdrawal and European responsibility: one commentary piece instead of two empty transcripts. (Medium, 3)
- [ ] **`book/summary.mdx` holds Polish working notes richer than the English articles** — especially on transport. Translate and absorb, then retire the file. (Medium, 3)

### Argument gaps found while reviewing

- [ ] **Nothing says how any of this is done politically** — no treaty change, unanimity, or procedure anywhere; every draft ends with "phased approach" and no mechanism. (High, 5)
- [ ] **Nothing says who pays** — no budget, financing model, MFF or eurobonds. `european-egovernment.mdx` claims 65-75% savings with no method. (High, 3)
- [ ] **No serious counter-argument anywhere** — sovereignty, the translation industry, states disadvantaged by English, French opposition. The sidecars notice this; the public pages do not. A manifesto that never states the strongest objection reads as propaganda. (High, 3)
- [ ] **Claims carry no evidence outside `european-english.mdx`** — the single biggest editorial weakness of the whole site. (High, 8)
- [ ] **Energy is absent entirely; defence lives only in two empty transcripts** despite `principles.md` naming security as a pillar; education is one sentence. (Medium, 5)

- [ ] **husky pre-commit runs full `astro check` (~50 s) and `git stash` on every commit** — `lint-staged` backs up state via stash (`package.json` lint-staged config, `.husky/pre-commit`), which `~/.claude/rules/worktrees.md` forbids inside worktrees; found while committing in `.claude/worktrees/vision` 2026-09-20. Make the check incremental or disable the stash backup (`--no-stash`). (Low, 2)

- [ ] **A `.internal` domain answers `curl` through the proxy but the browser gets Caddy's "No route"** — `idomains ensure eu-preview.internal proxy 4333` reported `created`, `idomains list` shows the entry, `curl -x localhost:2080 http://eu-preview.internal/vision` returns 200, and real Chrome (automation profile) renders `No route for eu-preview.internal` from Caddy itself on two attempts. So the browser reaches Caddy by a path the proxy-curl does not use (likely the HTTPS listener, or a stale PAC in that profile). This is the "proxy-curl proves nothing about the browser" trap in reverse, and it blocks every visual verification of a locally previewed site. Belongs to `internal-domains`/Caddy config, not to this repo — filed here because this repo is where it bit. Blocked visual QA of the rebuilt `/vision` page on 2026-09-20. (High, 5)

- [ ] **Light theme is unreadable** — most sections hardcode dark backgrounds (`bg-gray-900`, inline `background:` in `src/pages/index.astro`) while text colour comes from `--color-text-primary`; light mode gives dark-on-dark. Worked around by defaulting to dark (`src/layouts/Layout.astro:35`, commit d594a9a). Real fix: replace hardcoded colours with theme variables, then restore the system-preference default. (Medium, 5)
- [x] **Homepage below the frame still predates it** — "Why Digital Reform", 8 Benefits cards (6 without links, `src/pages/index.astro` Benefits section) and FAQ do not follow from the aging-Europe argument. Rewrite or cut so every block serves the frame. (High, 5) — rebuilt in `.claude/worktrees/home-v2`, branch `feat/home-v2`: frame → three layers → audience-based benefits → FAQ → CTA (`src/pages/index.astro`); old page kept at `src/pages/home-classic.astro` (preview-only).
- [ ] **One console error on eu.internal home: `ERR_NAME_NOT_RESOLVED`** for an external resource — not identified (seen 2026-09-21 in headless Chrome). (Low, 1)
- [ ] **`TimelineBlock.astro` renders unreadable white-on-white text** — `text-gray-900 dark:text-white` on a `bg-white dark:bg-gray-800/50` card, but the site is permanently dark-themed (`src/layouts/Layout.astro:35`, see the light-theme entry above), so the `dark:` variants apply and title/paragraph text is near-white on a white card. Found 2026-09-21 rendering `/why` after wiring `WhyDigitalReform` (which uses `TimelineBlock`) into `src/content/docs/why.mdx` for the home-v2 rework — screenshot `.plan/reports/2026-09-22-home-v2/why-timeline-detail.png`. Same root cause as the light-theme entry; fix together. Out of scope for `home-v2` (component is shared, outside its file allowlist). (Medium, 2)

- [x] **`dark:` Tailwind variants silently follow the OS, not the theme toggle** — `tailwind.config.js` never sets `darkMode`, so the default `media` strategy applies and any `dark:` class paints by `prefers-color-scheme` while the site's theme is driven by the `.dark` class (`src/layouts/Layout.astro:44`). Found via `src/components/TimelineContainer.astro:9` painting a dark slab into the light theme (fixed on `feat/why-page`). Sweep `grep -rn "dark:" src/` for the same bug elsewhere, and either set `darkMode: 'class'` in `tailwind.config.js` or ban `dark:` in `CLAUDE.md` § Theme tokens. FIXED 2026-09-25, commit 38e111d: `darkMode: 'class'` set. (High, 3)
- [ ] **`precommit-typecheck.mjs` only checks staged files, so implicit-any debt hides until a file is next touched** — a one-line href edit in `src/pages/docs/articles.astro` (branch `feat/book-routes`) surfaced 4 pre-existing implicit-any errors unrelated to the routing change; same pattern hit `docs/ideas.astro`, `docs/articles/index.astro`, `docs/index.astro`. Fixed on the spot by annotating `getCollection('docs', ...)` callbacks with `CollectionEntry<'docs'>`, but the debt exists in every untouched `docs/*.astro` page that still uses an unannotated callback. Consider a periodic full-project `astro check` in CI, or a shared typed helper for the common `getCollection('docs', (entry) => ...)` pattern. (Low, 3)
- [ ] **`docs/all.astro:151-152` link to `/book/egov-vison` and `/book/egov-challenges` (lowercase) but the content files are `eGov-vison.mdx`/`eGov-challenges.mdx`** — Astro's default slug lowercases the id, so these may already resolve, but verify against `dist/book/` output; both chapters are drafts so they 404 in production regardless (content-audit "links to draft pages"). Found while repointing `/docs/book/*` links to `/book/*` in `feat/book-routes`, not fixed (outside that task's scope — draft-content policy, not a route-canonicalization bug). (Low, 1)
- [ ] **9 links to draft `/book/<slug>` chapters in `docs/all.astro` and (out of my scope) `vision/language-integration.astro`, `content/docs/index.mdx`** — these chapters (`language-integration-administrative-implementation`, `media-cultural-integration`, `economic-growth-language-unity`, `unified-payment-zone`, `near-zero-transaction-costs`, `digital-receipts`, `european-blockchain-archives`, `european-elearning-system`, `predictive-healthcare-system`, `language`, `eGov-vison`) are `draft: true` and 404 in production per site policy (`CLAUDE.md` → "Content audit"). Either publish them or stop linking to them from production-visible pages; `content-audit.md` tracks the exact list. Surfaced (not caused) by `feat/book-routes`, which correctly repointed the same links from `/docs/book/*` to `/book/*` — the draft-404 was previously masked because `/docs/book/*.astro` rendered the entry directly via `getEntry`, bypassing the draft gate entirely (itself a bug, now fixed by making `/book/[...slug].astro` and `/book/index.astro` agree on the `IS_PREVIEW` gate). (Medium, 3)

- [ ] **A new book chapter is invisible on /book unless someone edits a hand-kept slug list** — `GROUPS` in `src/pages/book/index.astro:16-49` is an allowlist; a chapter that is not listed builds, routes and reads fine but never appears on the index, so the failure is silent. Derive the grouping from frontmatter (e.g. a `layer:` field in `src/content/config.ts`) and fall back to an "Unsorted" group, so a new chapter is visible by default and a missing layer is loud. Found 2026-09-25 while adding `the-total-state`. (Medium, 3)
- [ ] **Direct democracy has no safeguards written anywhere** — the site now demands it (`src/content/docs/book/direct-democracy.md`, `the-total-state.md`) without stating thresholds, what a referendum may not decide, minority protection, or the double-majority mechanics of the Swiss model it points at. First objection any reader raises. (High, 3)
- [ ] **Digital voting contradicts the site's own total-state argument** — `the-total-state.md` says a digital system carries a switch; the playbook proposes digital referendums. Needs the answer written: paper as the base, open code, voter-verifiable, no single operator. Switzerland suspended its own e-voting after vulnerabilities were found — verify and use it rather than waiting to be hit with it. (High, 3)
- [ ] **Technocratic movement vs sovereign citizens — unresolved tension** — the playbook proposes a technocratic political movement AND that citizens decide. Say which wins when they disagree (suggestion: technocrats prepare options and costs, citizens choose), or the page reads as rule by experts, which is what readers reject in Brussels today. (Medium, 2)
- [ ] **No first step for a reader who agrees** — "demand direct democracy" names no instrument. The European Citizens' Initiative (1M signatures, 7+ states) is weak and non-binding, which makes it a good first move: it either works or it publicly demonstrates the site's thesis. Needs checking and writing up. (High, 3)
- [ ] **Brexit is used as evidence but cuts both ways** — a reader will answer "Brexit WAS a referendum". Pre-empt it: a one-off plebiscite with decisions handed back to the same people is not direct democracy. (Medium, 1)
- [ ] **Missing topics in the vision** — who pays for people's daily life during the thirty-year transition; food and farming as security; water; who controls the AI the robots depend on; and above all the chip and rare-earth dependency Paweł named in one sentence, which is a precondition for the whole robotics layer and probably its weakest link. Each is a chapter or an honest "open question" section. (High, 8)

- [x] **Every page ships the same hardcoded `<meta name="description">`** — `src/layouts/Layout.astro:6-17` declares only `title` and hardcodes the description meta, so per-page descriptions in content frontmatter never reach the HTML. Found while writing `/manifesto` (passing `description` to Layout fails `astro check`). Fix: add an optional `description` prop defaulting to the current string. (Medium, 2) FIXED 2026-09-25, E004-T04 (commit 95593d3): Layout takes description/canonical/ogImage/noindex/type props.

- [ ] **`docs/articles/*`, `docs/ideas/*`, `privacy.astro`, `terms.astro` still hard-code dark-theme utilities** — `text-white`, `text-gray-*`, `prose-invert`/`dark:prose-invert` (found via `grep -rln "text-white\|prose-invert" src/pages/`, 2026-09-25). Same bug class as E004-T01, but these files were outside its named scope (`book/*`, `commentary/*`, `docs/[...slug].astro`, `transcripts/[...slug].astro`, `vision/*`). Needs the same treatment: replace with `theme-*`/`var(--color-*)` tokens. (Medium, 3)
- [ ] **`eu.zentala.io` remnants outside shipped code** — after E004-T05 (canonical domain → `zentala.eu`, commit 2820513), `eu.zentala.io` still appears in `content-old/sponsor&engage.md` (unused legacy folder, outside `src/`, not part of the build) and in `.plan/reports/2026-09-25-ui-seo-review/{01-inventory,02-content-seo-geo}.md` and `.plan/epics/E004-.../PLAN.md` (historical planning docs, correctly describing the old domain as context). None of these ship or affect the live site; left as-is rather than editing historical record docs mid-task. Delete `content-old/` outright if it's confirmed unused, or scrub it if it ever gets revived. (Low, 1)
- [ ] **`scripts/precommit-typecheck.mjs` scoped-tsconfig approach was unsound for this codebase** — FIXED 2026-09-25 (commit ff667f7, found while committing E004-T01): scoping `astro check`'s `include` to only the staged files excluded plain `.ts` helpers (`src/lib/commentary.ts`, `src/lib/vision-content.ts`) that `astro:content` collection typing depends on, making every collection entry type-check as `never` and failing commits on unrelated `.astro` pages with dozens of false-positive errors — reproduced even with every pending file staged, while the unscoped `astro check`/`yarn typecheck` passed with 0 errors. The script now always runs the full-project check; it's slower, but a scoped variant would need to compute the full transitive import graph of the staged files (not just the staged files themselves) to be sound — worth revisiting if the full check gets slow enough to matter. (Low, 2)
- [ ] **No fast gate for content-only changes** — verifying a new chapter costs a full `just build` (~91 s, 218 pages), though the only failure mode a markdown chapter has is frontmatter/schema. Add a `check-content` target running `astro check` without the static build. Found while writing `src/content/docs/book/safeguards.md` (2026-09-25). (Medium, 2)
- [ ] **Nationalists vs globalists has no home on the site** — Paweł's argument (the real divide is no longer left/right; globalists warn against nationalists and are partly right; nationalist governments are no guarantee either; therefore demand direct democracy rather than trusting the next party) is recorded in `.plan/vision/2026-09-25-answers-and-safeguards-dump.md` and appears on no page. It needs its own chapter or a section in /why; its sharpest formulations are contested claims about named governments, so it needs care. In political science this is the GAL-TAN / open-closed divide — worth naming. (High, 5)
- [ ] **ECI admissibility needs a real legal opinion before the initiative is launched** — Art. 11(4) TEU speaks of "a legal act of the Union" and a Treaty revision is not one, so an initiative demanding binding EU-wide referendums may be refused registration; the counter is that under Art. 48 TEU the Commission may itself propose Treaty amendment. This is the single load-bearing uncertainty in `src/content/docs/book/first-move.md`. Get a written opinion, or reframe the ask as a legal act the Commission can propose. (High, 3)
- [ ] **Decide the rights floor for referendums** — the safeguards chapter deliberately leaves open whether the limit on what a vote may decide is the whole ECHR or only its non-derogable core. Paweł's decision changes the text. (Medium, 1)
- [ ] **Two live route prefixes for the same chapter: `/docs/book/<slug>` and `/book/<slug>`** — both build (`dist/docs/book/designing-our-retirement/index.html` and `dist/book/.../index.html` both exist), so internal links are split between them with no canonical form: `src/content/docs/book/designing-our-retirement.md`, `political-path.md`, `robotic-reindustrialisation.md`, `european-english.mdx` use `/docs/book/`; newer chapters use `/book/`. Duplicate URLs for identical content also split any search ranking. Pick one, redirect the other, and record it in `zentala.eu/CLAUDE.md` under Content Guidelines. Found while writing `src/content/docs/book/education-and-migration.md` (2026-09-25). (Medium, 3)
- [ ] **Chapter `education-and-migration` is filed under "The Frame" for lack of a better group** — `src/pages/book/index.astro:17`. It is a policy proposal, not part of the three-layer frame; once the shorter-working-week and global-south chapters from `.plan/vision/2026-09-25-education-migration-workweek-dump.md` exist they probably want a group of their own ("People and Work"). (Low, 2)
- [ ] **`/transcripts/<entry>` still fails axe after E004 wave 1** — `.bg-red-900/30` box fails colour-contrast in both themes, and the YouTube `<iframe>` brings third-party aria violations (button-name, aria-allowed-attr, aria-prohibited-attr). Fix the box with tokens; replace the iframe with a click-to-load facade (thumbnail + button). Found by the after-audit, 2026-09-25. (Medium, 2)
- [ ] **Legacy redirects point at the absolute production URL** — `/docs/why` etc. redirect to `https://zentala.eu/why` (Astro redirect built from `site`), so on eu.internal the preview visitor lands on the old live site instead of the preview. Use relative redirect targets. Found by the after-audit, 2026-09-25. (Low, 1)
- [ ] **`support.astro` share buttons use brand hex colours** — `bg-[#0C6CA8]` etc. predate E004 (wave 1 only darkened them to pass contrast). Move to brand tokens in `_tokens.scss` under E004-T12. (Low, 1)
- [ ] **Book chapters mix `/docs/book/` and `/book/` link prefixes** — same pages reachable both ways, so each agent guesses; pick `/book/` and sweep (`src/content/docs/book/*.md`, `src/pages/book/index.astro`), then state the rule in `CLAUDE.md` → Content Guidelines. (Medium, 2)
- [ ] **`tests/timeline.spec.ts` fails against current `Timeline.astro` styling** — 5 Playwright specs assert stale Tailwind classes (`bg-blue-900`, `.bg-gray-800`, `rounded-xl`/`shadow-md`/`border-2`/`border-blue-400`) that no longer match the token-driven markup after commit 7ae3f40 (`fix(theme): token-drive ContentBox, Testimonials, Timeline, ...`). Pre-existing, unrelated to E004-T08; found while running `just test` for that task (2026-09-25). Update the assertions to the current token classes or drop the styling checks in favour of a rendered snapshot. (Low, 2)
- [ ] **The site's nomenclature does not fit what it now holds** (Paweł, 2026-09-25) — `/book` reads as one book, so "Book: Choosing People" sounds like a book titled Choosing People; what is actually there is a set of chapters, and some of them are closer to essays or proposals. The taxonomy is also unsettled: cross-cutting concepts vs tags vs topics, with `src/pages/tags.astro` already existing. Decide the words (chapters? essays? the programme?), decide whether a chapter belongs to a layer, a topic or a tag, and sweep the headings, the index and the frontmatter schema. Needs a proposal before any edits. (High, 5)
- [ ] **Undocumented 160-character cap on `description` in the docs collection** — an agent's first build failed on it with no hint in `CLAUDE.md`; state it in Content Guidelines (`src/content/config.ts`). (Low, 1)
- [ ] **`docs/articles/blockchain-state` (and likely other non-book `docs` entries) still ship two `<h1>`s** — `src/pages/docs/[...slug].astro` now uses the shared `ArticleLayout` (E004-T02), which renders one h1 from `entry.data.title`, but source files outside `book/` (e.g. `src/content/docs/articles/blockchain-state.md`) still open with a markdown `# Title` line. E004-T02's acceptance criterion was scoped to `book/*.md(x)` only (19 files, all fixed); the same sweep needs repeating across `docs/articles`, `docs/ideas`, `docs/reference`, `docs/support`, `docs/why`. Found via `grep -o "<h1" dist/docs/*/*/index.html` after the T02 build, 2026-09-25. (Medium, 2)
- [ ] **`wt-remove` refuses to delete `.plan/worktrees/E004-T02` with "a process holds" the directory, but no process referencing that path was found** (`Get-CimInstance Win32_Process` filtered on the path returned nothing after killing every stray `node.exe`/`astro dev`/`serve` process I had started there; `handle64` is not installed on this machine to confirm the actual handle owner). The worktree and its branch `feat/E004-T02` are left in place — work is already merged to `main` (commit `58613c4`) and safe, but the worktree occupies disk and the branch needs a manual `wt-remove`/`git branch -d` once the lock clears or `handle64`/Sysinternals is available to identify the holder. Found 2026-09-25 closing out E004-T02. (Low, 1)

- [ ] **tests/timeline.spec.ts fails against current Timeline.astro/TimelineBlock.astro markup** — selectors like `#timeline h2.text-3xl.text-center.text-white` and `.h-2.bg-blue-700` no longer match current component output; pre-existing before E004-T10 (Timeline.astro untouched by that task). (Importance: Low, Points: 2)

## Found by the E006 reconnaissance (2026-09-25)

- [x] **`src/pages/docs/[...slug].astro` generates a shadow `/docs/<slug>` URL for every non-draft docs entry** — no folder filter, so every chapter, `/why` and `/manifesto` exist twice; this is the generator that keeps recreating the duplicate-route problem the `/docs/book/*` redirects only papered over. Restrict it to the trees that belong under `/docs` (ideas, articles, reference) or redirect. (High, 3) FIXED 2026-09-25, E006-T08 (commit 0b7d8a4): catch-all now filters `entry.id` to `ideas/`, `articles/`, `reference/` only; a 43-row `redirects` table in `astro.config.mjs` (commit 46414df) covers the retired `/docs/book/*`, `/docs/why*` and `/docs/for-you` paths, generated from `src/content/docs/book/` so every chapter — not just the 13 that had a wrapper file — gets a real redirect instead of a 404.
- [x] **`src/content/docs/book/principles.md` is still published and is word-for-word the pre-frame prose cut from `/principles`** — "last demographic surplus generation", "four retirees rely on one working individual" (an invented figure). Retire or rewrite; `src/pages/docs/book/principles.astro` already redirects to the page, not the chapter, which suggests it was meant to go. (High, 2) FIXED 2026-09-25, E006-T08 (commit f49d109): file deleted; `/docs/book/principles` and `/book/principles` both redirect to `/principles`, and `docs/index.mdx:18` now links there directly.
- [ ] **Invented figures in the 2024–25 chapters** — "65–75% savings" in `european-egovernment.mdx`, the retiree ratio in `principles.md`; they break the no-invented-numbers rule and contradict `the-total-state.md`. Remove or source. (High, 2)
- [ ] **Agent research attributed to Paweł in first person** — `safeguards.md` ("I propose"), `steering.md` (Ashby, Beer), `who-it-earns-for.md` ("I found three"), `first-move.md` ("my draft"), `political-path.md:110-118` (a military argument for English absent from every dictation). Re-voice as "proposed/found", add a provenance note per chapter; rule goes into `src/content/AGENTS.md` (E006 D2). (High, 5)
- [ ] **Cross-chapter contradictions (7)** — the sharpest: "not communism" (`who-it-earns-for.md:170`) vs "in effect communist" (`cheap-is-wealth.md:140`); two threshold figures across three chapters; `redesigning-the-state.md:180-182` says longevity may mean no labour problem at all, which dissolves the site's premise. Full list in `reports/2026-09-25-e006/R4-editorial-audit.md`. Paweł decides, then one sweep. (High, 3)
- [ ] **No chapter argues WHY English, in his voice** — `european-english.mdx` is a 2,400-word standards spec; the argument lives only in `.plan/vision/aging-society-frame.md:44-70`. The language layer is the declared floor and has no floor chapter. Check `european-english.sidecar.mdx` first. (High, 5)
- [ ] **`tags` are free text and inconsistent** (`direct-democracy` vs `direct democracy` are two tags on `/tags`); repurpose as the governed concept list for tooltips per the IA document. (Medium, 2)
- [ ] **12 components are dead or showcase-only**, three timelines, four card variants — merge plan in `reports/2026-09-25-e006/R2-components-and-patterns.md`. (Medium, 5)
- [ ] **`Newsletter.astro` interpolates an env var literally into the form URL**. (`DisplayTitle.astro`'s `set:html` heading-tag issue was fixed in E004-T11 — it now uses `const Tag = \`h${level}\``.) (Low, 1)
- [ ] **`/ui/index.astro` is not yet an exhaustive living style guide** — E004-T11 added a Surface variant grid, Timeline compact/numbered demos and a states section, but most of the existing ~880-line page still uses hardcoded dark Tailwind classes instead of theme tokens and does not render every component/variant in an explicit light AND dark section as the acceptance criterion asks. A full rewrite is a separate follow-up (`src/pages/ui/index.astro`). (Medium, 5)
- [ ] **`just test`/`npm test` cannot run when another dev server already occupies port 4333** — `playwright.config.ts` hardcodes `baseURL`/`webServer.url` to `http://localhost:4333` with `reuseExistingServer: true`; during E006-T06 a stray `astro dev` process from the main checkout's own `node_modules` (started 2026-09-25 15:51, unrelated to this task) was bound to 4333 and answering with HTTP 500, so Playwright's readiness probe timed out after 60s instead of starting its own server. With several ts-dev worktrees running in parallel per epic, port collisions on the shared default dev port are likely to recur. Consider parametrising the port (env var) or documenting a per-worktree port convention in `AGENTS.md`. Found while verifying E006-T06 (2026-09-25); worked around by screenshotting against a manually started `astro preview --port 4790` instead of running the full spec suite. Recurred 2026-09-25 during E006-T08 (PID 56776, `[::1]:4333`, same HTTP 500 symptom, same workaround with `astro preview --port 4855`) — a second confirmation that this needs the port parametrised rather than one-off worked around. Recurred a third time during E006-T07 — same PID 56776, still bound to `[::1]:4333` from the main checkout, `Get-CimInstance Win32_Process` confirms `astro dev --port 4333` running there since 15:51; worked around with a local, uncommitted `playwright.local.config.ts` (port 4341, deleted before commit) for `tests/timeline.spec.ts`, and `astro preview --port 4343` for the manual crawl. Three independent tasks hitting the identical PID is strong evidence this needs the port parametrised (e.g. `PLAYWRIGHT_PORT` env consumed by both `playwright.config.ts` and `justfile`'s `dev`/`preview` targets), not another one-off workaround. Recurred a fourth time during E006-T10 (2026-09-25) — same PID 56776, still `[::1]:4333`, still HTTP 500; left untouched rather than worked around this time, since T10's write set (`scripts/`, `package.json`, `justfile`, `.gitignore`) never renders a page, so the risk of a real regression was near zero — verified instead with `astro preview --port 4791` (200 on `/`, `/book/the-total-state/`, `/why/`), then killed that preview process by port. `npm test` itself was left NOT_CHECKED for this task. (Low, 2)
- [ ] **`scripts/deploy-internal.sh:16` calls `astro.js build` directly instead of `npm run build`** — it bypasses both `astro check` and, as of E006-T10, `node scripts/link-graph.mjs`, so a fresh `eu.internal` deploy ships without `src/data/link-graph.json`; once `/site-map` (T14) or the glossary page reads that file at build time, the internal preview build will break (or silently render an empty graph) while `just build` stays green. Either call `npm run build` with `SITE_PREVIEW=true` or add the missing steps explicitly. Found 2026-09-25 while verifying E006-T10 — out of that task's write set (`astro.config.mjs`/`scripts/deploy-internal.sh` are explicitly excluded). (Medium, 2)
- [ ] **A module-scope `const` read only inside `getStaticPaths`'s content-collection filter callback is dropped by Astro/Vite's chunking** — `PUBLISHED_PREFIXES` declared above `export async function getStaticPaths()` in `src/pages/docs/[...slug].astro` built fine under `astro check`/dev server but threw `ReferenceError: PUBLISHED_PREFIXES is not defined` from the generated `dist/chunks/_...slug___*.mjs` at `astro build` time — the filter callback passed to `getCollection()` gets bundled into its own chunk and the module-scope binding isn't always pulled in with it. Fix: declare the constant/array inside the function body, not at module scope. Found 2026-09-25 while building E006-T08; worth a `~/.claude/rules/typescript.md` note since it's a silent-until-build-time trap with no typecheck/dev-server symptom. (Medium, 2)
- [ ] **`just audit` reports 8 pre-existing "links to missing pages" unrelated to route cleanup** — `src/content/docs/index.mdx:34-35` links two book chapters that were never authored (`/book/language-integration-european-politics/`, `/book/language-integration-national-states/`, present since the original Starlight-era content, commit `2a570a2`); `src/content/docs/ideas/index.mdx:87,108,122,157,325` links three ideas pages that don't exist (`ideas/technology/autonomous-robotics-system`, `ideas/governance/customized-local-law`, `ideas/agriculture/autonomous-farming`) plus `/docs/ideas/all` twice; `src/components/Footer.astro:31` links `/sitemap.xml`, which `@astrojs/sitemap` never writes (it writes `sitemap-index.xml` + `sitemap-0.xml` — the same gap IA §10 row 9 names, owned by T10/T14's `/site-map` page). None of these are new; verified against base commit `8b75d5a` before E006-T08 touched anything. Either author the missing chapters/ideas or delete the links; fix the Footer link when `/site-map` lands. Found 2026-09-25 while verifying E006-T08's "0 dead internal links" acceptance criterion. (Medium, 2)
- [ ] **`/ui` (the living style guide) throws 9 `pageerror: Failed to resolve module specifier 'chart.js'` per page load** — `src/components/EUStatisticsChart.astro:73` does `await import('chart.js')` inside a client `<script>`; in both `astro dev` and the static build/preview output the bare specifier is never resolved to a bundled URL, so every one of the three `EUStatisticsChart` examples on `/ui` (bar/pie/line, `ui/index.astro`'s "Charts" subsection) fails with a console `pageerror`. Pre-existing and out of E006-T07's scope (DESIGN.md Migration step 10 owns `EUStatisticsChart`/retiring `InteractiveSection`/`EuropeMap`, not steps 4–7); confirmed via `git diff main -- src/components/EUStatisticsChart.astro` = 0 lines. Found 2026-09-25 running E006-T07's required 7-page × 2-theme console-error crawl. (Medium, 2)
- [ ] **`/ui`'s Hero example links a placeholder image that 404s** — `src/pages/ui/index.astro:583,598` sets `image={{ file: "/images/hero-image.jpg", ... }}` / `image="/images/hero-image.jpg"`; no such file exists under `public/`, so the Hero component example 404s once per page load in both dev and the built preview. Pre-existing, untouched by E006-T07's diff. Either add a placeholder asset or point the example at a real one (e.g. `../../assets/eu.png`). Found 2026-09-25 during the same crawl. (Low, 1)
- [ ] **`ArticleGrid.astro` has no named track for the 2/3–1/3 margin position DESIGN.md's Layout table calls for** — `SideNote`, `KeyFigure aside`, `PullQuote aside` and `EvidenceBox aside` (E006-T09) all fall back to a `float: right` at ≥1024px inside their own scoped styles, the same workaround `EvidenceBox` (E006-T07) already used, because `ArticleGrid.astro` (E006-T06) only names `content`/`wide`/`full` tracks, not the `content-end`→`full-end` margin DESIGN.md's "2/3–1/3" row describes. Works today (verified in both themes, `.plan/reports/2026-09-25-e006/T09-{light,dark}.png`), but four components now duplicate the same float-based placement instead of sharing one grid-column class; a `span-margin` track added to `ArticleGrid` would let all four drop the float CSS. Not this task's write set (`ArticleGrid.astro` is owned by T06/T07). (Low, 2)
- [ ] **E006-T09 acceptance criterion 3 ("`dist/ui/content/index.html` does not exist after the production build") is not achievable with this repo's existing `IS_PREVIEW`-gate pattern** — every `Astro.redirect()`-gated static page (`ui/index.astro`, `home-classic.astro`, and now `ui/content.astro`) still gets a one-line `noindex` meta-refresh stub written to `dist/` in a production build; Astro's static builder always emits a file for a statically-named page route regardless of runtime logic. Verified `dist/ui/index.html` has the identical stub shape in a plain production build, so this is pre-existing repo behaviour, not something E006-T09 introduced. The literal fix (a dynamic `[page].astro` with `getStaticPaths` returning `[]` outside preview) would rename the file outside every such page's declared filename and diverge from the established convention across three pages at once — flagging instead of unilaterally changing the pattern. Full reasoning in `E006-T09.md` → "Notes on acceptance criterion 3". (Low, 1)
