# E006 Journal

## Session 2026-09-25 (epic-close, interrupted)

- **Goal**: E006: Design system, editorial standard, information architecture — waves 3
  (infrastructure) and 4 (pages).
- **Done**:
  - E006-T06 — design tokens, `ArticleGrid`, article typography per DESIGN.md Migration
    steps 1–2. `_tokens.scss` + `tailwind.config.js` gain the proposed type/spacing tokens;
    `ArticleGrid.astro` (content/wide/full tracks) added and `ArticleLayout.astro` migrated
    onto it; `.prose` heading weights split 700/400, H2 at `--text-3xl`, h4–h6 to Inter 600
    (fixed at the `Layout.astro` global level, which previously forced serif on all six
    heading levels); prose links underlined. `just typecheck` (0 errors) and `just build`
    (274 pages) green before/after merge; Playwright confirmed `--article-measure` = 66ch in
    both themes on `/book/direct-democracy/`. Merged `feat/E006-T06` → main (`7f7fc79`).
  - E006-T07 — component consolidation + `/vision` anchors. Generalised
    `why/WhySection`→`Section`, `why/WhyLead`+`manifesto/ManifestoLead`→`Lead`,
    `why/WhyEvidenceNote`→`EvidenceBox`, `manifesto/ManifestoDemand`→`Callout`; dissolved
    `src/components/why/` and `manifesto/` except `why/WhyAnswer.astro`. Retired
    `WhyDigitalReform`, `GradientSection`, `StakeholderBenefits`, `Logo`,
    `why/WhyCardGrid` (no importers). Trimmed `Card`, `CardGrid`, `FAQ`,
    `vision/LayerCard`. Added the four `/vision` anchors and gated `/ui` on
    `IS_PREVIEW`. `build`/`typecheck`/`audit` green; `tests/timeline.spec.ts` (6/6)
    unchanged; all 7 required pages × 2 themes verified with zero console errors except two
    pre-existing, out-of-scope `/ui` bugs (chart.js dynamic import, placeholder
    `hero-image.jpg` 404) — confirmed 0-diff on the responsible files, filed in
    BACKLOG.md rather than fixed. Merged `--no-ff`.
  - E006-T08 — route cleanup and the redirect table, merged `ea95981`. Catch-all now
    filters to `ideas/articles/reference` only; 43-row redirects table generated by
    `readdirSync` over `book/` covers every chapter; 13 wrapper pages and the 3
    `/vision/<layer>` pages deleted; `book/principles.md` deleted; both eGov chapters
    `draft: true`; `docs/all.astro` rewritten to a thin token-styled archive index. Found and
    fixed a genuine Astro/Vite build-time bug (module-scope `const` dropped from a
    `getStaticPaths` filter chunk) along the way. Confirmed Astro 4's static redirects
    preserve `#fragments`, so `/vision/<layer>` became config-table entries. Playwright
    could not run (port 4333 occupied by a concurrent worktree, not killed); worked around
    with `astro preview` + curl/dist inspection — not equivalent to a rendered-browser
    check. Task-status frontmatter was left `pending` after the merge; corrected to
    `completed` in this closing session (commit `101f24e`).
  - E006-T09 — all 11 DESIGN.md content components (`Objection`, `ObjectionList`,
    `MeansForYou`, `ComparisonTable`, `Playbook`, `KeyFigure`, `SideNote`,
    `ProvenanceNote`, `PullQuote`, `SourceList`, `Figure`) plus the `IS_PREVIEW`-gated
    `/ui/content` gallery. `ProvenanceNote` implements the authoritative five-field shape
    from `src/AGENTS.md` §3 (Source/Dictated/Added/Cut/Open), superseding DESIGN.md's
    simpler sketch per the task's own instruction. Built, typechecked, screenshotted both
    themes, merged, worktree/branch cleaned up.
  - E006-T12 — term tooltips, `/glossary`, tags retirement. Its prerequisite E006-T05 had
    not actually shipped despite the plan assuming it had; implemented T05's blocking
    slice (glossary collection + schema) on the same branch and left the rest of T05
    explicitly open. Glossary collection (19 entries), `TermRef.astro` +
    `rehype-glossary-terms.mjs` auto-marking, `/glossary` grouped by layer, `/tags`
    deleted with redirects. Playwright hover/Esc verified both themes, zero console
    errors. Merged, task-status completed.
  - E006-T13 — `/why` and `/manifesto` onto `ArticleLayout` (66ch measure, old
    68rem/52rem frames gone). `EUStatisticsChart` reads design tokens at render time
    (zero hex/rgba literals, grep-verified); `InteractiveSection`, `EuropeMap`,
    `Testimonials` deleted (no sourced data behind any of the three, per DESIGN.md's "or
    nothing" migration fallback). Remaining hard-coded literals in `DisplayTitle`,
    `Hero`, `Newsletter`, `EvidenceBox`/`Section`/`FAQ`/`ui-content` now read tokens
    (repo-wide grep confirms). `/vision` lists each layer's published chapters under its
    anchor. One deviation: `WhyAnswer.astro` was **not** deleted — `why.mdx` (owned by
    T19) still imports it 6 times; documented in BACKLOG.md as a plan-sequencing gap for
    T19 to close. Merged `ce6bd88`.
  - E006-T14 — `/site-map` page (seven-column wayfinding list + deterministic inline SVG
    link-graph, CSS-only `:has()` highlighting, `<figure>`+`<details>` fallback) and six
    new IA shape audits in `scripts/content-audit.mjs`. Built in an isolated worktree
    while E006-T05 was still pending, so both the page and the audits degrade gracefully
    rather than crashing or false-passing; re-verified after T05/T12 merged concurrently.
    Merged with no conflicts.
- **Not done / blocked** (recorded so the next session does not re-discover them):
  - **E006-T05** (in-progress, `11af49b`) — landed its blocking slice (glossary
    collection + schema) via T12's branch, but the rest of the task (full frontmatter
    sweep, `LAYERS`/`KINDS` constants for downstream consumers) never shipped. Status is
    `in-progress`, not `completed`.
  - **E006-T10** (completed, `61d55f5`) — link-graph and design-lint scripts shipped and
    merged (`b935946`); listed here only because it fell inside w3 scope. No blocker.
  - **E006-T11** (blocked, wave 4) — chapters index from `layer`, eyebrow, `ChapterFooter`.
    Explicitly blocked on E006-T05 merging with `LAYERS`/`KINDS`/`layer`/`kind`/`order`
    landed on `main`; that dependency is still open.
  - **Wave 5** (T15–T19: floor chapter, chapter rewrites, eGov merge, top-level
    one-home sweep) was never started — outside the requested w3/w4 scope.
- **Decisions**: none rose to ADR level this session; no new architectural decision was
  introduced without one. `.plan/ARCH.md` does not exist in this repo (never created by an
  earlier epic) — nothing to reconcile against; noted as a pre-existing gap, not created
  here since no concrete architecture content was asked for.
- **Findings this session**: 1 (T08 task-status frontmatter left `pending` after its own
  merge — corrected).
- **Improvements logged**: 0 — `IMPRO.md` does not exist for this epic; nothing was staged
  for Paweł triage.
- **Weryfikacja (per-task, as reported by the closing tasks)**: PARTIAL. Full-scope
  commands green: `corepack yarn typecheck` (0 errors), `corepack yarn build` (exit 0, 85
  pages at T14's check; 274 at T06/T07's earlier check — page count moved as routes were
  added/retired across the wave), `corepack yarn run audit` (exit 0), `node
  scripts/link-graph.mjs` (28 nodes / 125 edges), `node scripts/design-lint.mjs` (176
  non-strict violations, exit 0 as designed). No automated test suite beyond
  `tests/timeline.spec.ts` (`npm test`), which several tasks (T08, this session) could not
  run against a live browser.
- **Nie sprawdzono** (carried forward, not re-attempted this session): `npm test`
  (Playwright) against the merged tree; interactive term-tooltip behaviour beyond static
  markers; wave 5 tasks (not started, so the epic's "every chapter uses a non-prose
  component" criterion is not yet judgeable); whether the 8 missing-page links and 13 dead
  external links in `content-audit.md` are new regressions vs. a pre-wave-3 baseline
  (no baseline audit exists to diff against); dark mode / mobile-width layout across the
  wave; whether redeploying `eu.internal` would resolve `/site-map` and `/glossary` 404s
  reported by T14 (a deploy action, not verification).
- **Review readiness**: NOT CLEARED — `review-log status` reports every review kind
  (`review`, `plan-eng-review`, `plan-design-review`, `plan-ceo-review`, `outside-voice`,
  `qa`) as `NEVER RUN` against `HEAD e4ad71b4`. Recorded as `issues` below per the closing
  protocol rather than left silent.
- **Worktree cleanup**: 0 removed, 4 relevant task branches already gone from git history
  before this session started (T06, T07, T08, T09, T12, T13, T14 worktrees/branches were
  already merged and removed by their own closing sessions); 0 failed; 31 worktrees remain
  on the machine, none E006-named (`git branch --list "feat/E006*" -a` → empty;
  `.plan/worktrees/` and `.claude/worktrees/` contain no E006 entry) — nothing left to
  clean for this epic.
- **Blockers preventing epic close**: E006-T05 incomplete (in-progress); E006-T11 blocked
  on T05; wave 5 (T15–T19) not started. Epic status stays `in progress`; not written to
  `.plan/HISTORY.md`.
- **Next**: land the rest of E006-T05 (full frontmatter sweep + exported `LAYERS`/`KINDS`),
  which unblocks E006-T11; then resume wave 5. Run `/review` before treating any of this
  wave's shipped code as reviewed — the board has never run against this tree.
