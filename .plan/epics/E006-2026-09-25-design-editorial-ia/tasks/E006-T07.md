---
formatVersion: 1
type: improvement
id: E006-T07
title: Component consolidation and /vision anchors
epic: E006
status: pending
priority: high
effort: medium
agent: ts-dev
points: 5
model: standard
confidence: 4
summary: "Rename the why/manifesto components into the generic Section, Lead, EvidenceBox and Callout, merge the three timelines into one data-driven Timeline, retire dead components, calm the cards, and add the layer anchors /vision needs."
created: 2026-09-25
wave: 3
completed_at: null
commits: []
verification_evidence: []
branch: feat/E006-T07
---

# E006-T07: Component consolidation and /vision anchors

Covers `DESIGN.md` Migration steps 4, 5, 6, 7 and the anchors row of IA §10 (`C:/Users/zentala/code/zentala.eu/.plan/INFORMATION-ARCHITECTURE.md`). Read the DESIGN.md Components contracts for Section, Lead, EvidenceBox, Callout, Timeline, Card, CardGrid, FAQ, LayerCard before writing.

## Write set

- `src/components/**` EXCEPT `ArticleGrid.astro` (E006-T06) and the new files E006-T09 creates (Objection, ObjectionList, MeansForYou, ComparisonTable, Playbook, KeyFigure, SideNote, ProvenanceNote, PullQuote, SourceList, Figure).
- Importers of the renamed or deleted components: `src/pages/why.astro`, `manifesto.astro`, `principles.astro`, `index.astro`, `home-classic.astro`, `benefits.astro`, `support.astro`, `src/pages/vision/index.astro`; import lines ONLY in `src/content/docs/why.mdx` and `manifesto.mdx`.
- `.plan/reports/2026-09-25-salvaged-from-homepage.md` (append, main checkout).

Do not touch `src/styles/*`, layouts, `src/content/config.ts`, `astro.config.mjs`, `src/pages/docs/**`, or the `src/pages/vision/<layer>.astro` wrappers (other tasks own them).

## Steps

1. Step 4: `WhySection` → `Section`, `WhyLead` + `ManifestoLead` → `Lead`, `WhyEvidenceNote` → `EvidenceBox`, `ManifestoDemand` → `Callout` (with the `definition` variant that registers an anchor id, DESIGN step 9). Delete the `why/` and `manifesto/` folders except `why/WhyAnswer.astro` (E006-T13 retires it). Update every importer.
2. Step 5: retire `ContentBox`, `Logo`, `ReasonBlock`, `SearchBar`, `StakeholderBenefits`, `WhyCardGrid`, `Timeline` (horizontal), `GradientSection` — grep usages first; `home-classic.astro` is preview-only but must still build.
3. Step 6: `TimelineContainer` + `TimelineBlock` → one data-driven `Timeline.astro` (line ends at the last circle, as today). `WhyDigitalReform`: quote its prose verbatim into the salvage report, then delete it.
4. Step 7: `Card` loses `align`, `gradient`, hover scale; `CardGrid` loses `stagger`, gains `align`; `FAQ` loses container box, shadow, hue; `LayerCard` radius and border per contract.
5. Anchors: sections in `src/pages/vision/index.astro` get ids `language`, `digital`, `robotics`, `who-captures-the-gains` (`layers-title` stays).
6. `just build`; crawl `/`, `/why`, `/manifesto`, `/vision`, `/benefits`, `/home-classic` in both themes with Playwright (`chromium.launch({ channel: 'chrome' })`); zero console errors.

## Acceptance

- `just build` exits 0; `ls src/components/why src/components/manifesto` shows only `why/WhyAnswer.astro`.
- `src/components/{Section,Lead,EvidenceBox,Callout,Timeline}.astro` all exist; `TimelineContainer.astro`, `TimelineBlock.astro`, `WhyDigitalReform.astro`, `GradientSection.astro`, `ContentBox.astro`, `ReasonBlock.astro`, `StakeholderBenefits.astro`, `SearchBar.astro`, `Logo.astro` are gone.
- `grep -c 'id="who-captures-the-gains"\|id="language"\|id="digital"\|id="robotics"' src/pages/vision/index.astro` = 4.
- `grep -rn "hover:scale\|stagger" src/components/Card.astro src/components/CardGrid.astro` returns nothing.
- Crawl report: 6 pages × 2 themes, 0 console errors.

## Working rules (every E006 task)

- Repo root `C:/Users/zentala/code/zentala.eu`. Standards `DESIGN.md` and `src/AGENTS.md` are tracked and present in your worktree. Plan documents live under the main checkout's `.plan/` directory — read them by absolute path from `C:/Users/zentala/code/zentala.eu/.plan/`.
- First command in the worktree: `just setup`. Build: `just build`. Audit: `just audit`. Typecheck: `just typecheck`.
- Stage by explicit path (`git add <file>`), never `git add -A` / `git commit -a`; never stage the worktrees directory.
- Frontmatter `description` is 70–160 characters (schema-enforced).
- Dev-only features are gated on `IS_PREVIEW` (`src/lib/preview.ts`), never on `import.meta.env.DEV`.
- Preview servers: start on a free port, kill BY PORT when done; never kill by process name.
- Colours, sizes, radii, fonts come from tokens (`src/styles/_tokens.scss`, `tailwind.config.js`); no hard-coded values in components.
- Both themes must render (`.dark` class on `<html>`); check light and dark.
- Stay inside your write set; another task in this wave owns the other files. A change needed elsewhere → one entry in the main checkout's `.plan/BACKLOG.md` with path:line.
