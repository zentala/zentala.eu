---
formatVersion: 1
type: feature
id: E006-T10
title: Link-graph and design-lint scripts
epic: E006
status: completed
priority: medium
effort: small
agent: ts-dev
points: 3
model: standard
confidence: 4
summary: "Generate the site's link graph at build time and add the design-lint script that counts violations of the testable DESIGN.md rules, both failing loudly on empty input."
created: 2026-09-25
wave: 3
completed_at: 2026-09-25
commits: [61d55f5]
verification_evidence: []
branch: feat/E006-T10
updated: 2026-09-25
---

# E006-T10: Link-graph and design-lint scripts

Covers IA T09 (§8.2 "Data", `C:/Users/zentala/code/zentala.eu/.plan/INFORMATION-ARCHITECTURE.md`) and the "Checks" item of DESIGN.md "Proposed additions".

## Write set

- `scripts/link-graph.mjs` (new), `scripts/design-lint.mjs` (new)
- `package.json` (`build` script and a `lint:design` script), `justfile` (`lint-design` recipe), `.gitignore` (`src/data/link-graph.json`), `src/data/.gitkeep`

Do not touch `scripts/content-audit.mjs` (E006-T14) or `astro.config.mjs` (E006-T08).

## Steps

1. `scripts/link-graph.mjs` per IA §8.2: nodes = every published page of the core surface (top-level pages, chapters, `/why`, `/manifesto`, later `/glossary`, commentary when public) with `id`, `title`, `layer`, `kind`, `inbound`, `outbound`; edges = internal links in the entry bodies and in `src/pages/*.astro` + `src/lib/vision-content.ts`, excluding header/footer/generated-footer links. Parse frontmatter tolerantly (`layer`/`kind` may be absent — E006-T05 adds them in parallel; use `gray-matter` if present in `node_modules`, otherwise add it as a devDependency with `corepack yarn add -D gray-matter`). Write `src/data/link-graph.json`. Exit non-zero when nodes < published chapters or edges = 0. Drafts excluded unless `SITE_PREVIEW=true`.
2. Wire it: the npm `build` script runs `node scripts/link-graph.mjs` first, then the existing chain (astro check, astro build, sitemap rename) unchanged. `scripts/deploy-internal.sh` calls the npm build, so it inherits it — verify by reading it, do not edit it.
3. `scripts/design-lint.mjs`: runs DESIGN.md rules 1, 2, 6, 7, 8, 13, 14, 15, 16, 19, 20 and 22 over `src/` (and `dist/` where a rule needs built HTML — e.g. more than 4 consecutive `<p>` without a structural element), prints a count per rule with the offending `path:line`s, exits non-zero when zero files were processed. Non-zero counts do NOT fail the run yet (the codebase is mid-migration); a `--strict` flag makes them fail.
4. `npm run lint:design` and `just lint-design` work; `just build` green.

## Acceptance

- `just build` exits 0 and `src/data/link-graph.json` exists with ≥ 20 nodes and ≥ 50 edges.
- `node scripts/link-graph.mjs` pointed at an empty temp content dir (env or flag) exits non-zero.
- `node scripts/design-lint.mjs` prints 12 rule lines with counts; `node scripts/design-lint.mjs --strict` exits non-zero today (there are violations).
- `git check-ignore src/data/link-graph.json` succeeds.

## Working rules (every E006 task)

- Repo root `C:/Users/zentala/code/zentala.eu`. Standards `DESIGN.md` and `src/AGENTS.md` are tracked and present in your worktree. Plan documents live under the main checkout's `.plan/` directory — read them by absolute path from `C:/Users/zentala/code/zentala.eu/.plan/`.
- First command in the worktree: `just setup`. Build: `just build`. Audit: `just audit`. Typecheck: `just typecheck`.
- Stage by explicit path (`git add <file>`), never `git add -A` / `git commit -a`; never stage the worktrees directory.
- Preview servers: start on a free port, kill BY PORT when done; never kill by process name.
- Stay inside your write set; another task in this wave owns the other files. A change needed elsewhere → one entry in the main checkout's `.plan/BACKLOG.md` with path:line.
