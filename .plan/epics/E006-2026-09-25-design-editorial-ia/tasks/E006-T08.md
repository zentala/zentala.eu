---
formatVersion: 1
type: improvement
id: E006-T08
title: Route cleanup and the redirect table
epic: E006
status: pending
priority: high
effort: medium
agent: ts-dev
points: 5
model: standard
confidence: 5
summary: "Stop the /docs catch-all from publishing a twin of every chapter, replace 13 redirect wrappers with one redirects table, retire the pre-frame principles chapter, hide the mis-cased eGov twins, and send the legacy /vision layer URLs to their anchors."
created: 2026-09-25
wave: 3
completed_at: null
commits: []
verification_evidence: []
branch: feat/E006-T08
---

# E006-T08: Route cleanup and the redirect table

Covers IA §9 steps 1, 2, 3, 4 (draft + redirects part), 7, 8 and the `docs/index.mdx:18` fix (`C:/Users/zentala/code/zentala.eu/.plan/INFORMATION-ARCHITECTURE.md`). NOT steps 5, 6, 9 (E006-T11/T12) and NOT the eGov content merge (E006-T18).

## Write set

- `src/pages/docs/[...slug].astro`, `src/pages/docs/all.astro`, `src/pages/docs/why.astro` (delete), `src/pages/docs/for-you.astro` (delete), `src/pages/docs/book/*.astro` (delete all 11), `src/content/docs/index.mdx`, `src/content/docs/all.mdx`
- `astro.config.mjs` (the `redirects` table)
- `src/content/docs/book/principles.md` (delete); `src/content/docs/book/eGov-vison.mdx` and `eGov-challenges.mdx` (frontmatter `draft: true` only)
- `src/pages/vision/digital-integration.astro`, `language-integration.astro`, `robotic-reindustrialization.astro` (delete or reduce to wrappers, step 7)

Do not touch `src/content/config.ts` (`customSlug` removal is E006-T12), `src/pages/vision/index.astro` (E006-T07), `src/pages/book/*` (E006-T11).

## Steps

1. Step 1: the catch-all publishes only `entry.id` under `ideas/`, `articles/`, `reference/`; drop the `customSlug` branch and the `console.log` scaffolding.
2. Step 2: one `redirects` table in `astro.config.mjs` — `/docs/book` → `/book`; `/docs/book/<slug>` → `/book/<slug>` for the 11 wrapped slugs; `/docs/why` and `/docs/why-reforms` → `/why`; `/docs/for-you` → `/benefits`; `/docs/european-english` and `/docs/language-official-proposal` → `/book/european-english`; `/book/principles` → `/principles`; both spellings of both eGov slugs → `/book/european-egovernment`. Delete the 13 wrapper files. (`/tags` → `/glossary` and `/book/language` → `/book/one-working-language` are E006-T12's rows — leave a comment marking where they go.)
3. Step 3: delete `book/principles.md`; `docs/index.mdx:18` → `/principles`.
4. Step 4 (partial): set `draft: true` on both eGov files (they contradict `the-total-state.md:72-75`; the merge is E006-T18); fix `docs/all.astro:151-152`.
5. Step 7: `/vision/<layer>` → `/vision#<layer>`. Test whether Astro 4's static `redirects` keep the fragment in the generated stub; if not, keep three minimal wrappers with `Astro.redirect('/vision#language', 301)` and record the finding in the task report.
6. Step 8: `docs/all.astro` derives its lists from the collection or shrinks to the archive listing; the pre-frame description at `:9,13` goes.
7. `just build`; `just audit`; inspect `dist/`.

## Acceptance

- `just build` exits 0; `just audit` reports 0 dead internal links.
- `src/pages/docs/book/` no longer exists; `src/pages/docs/why.astro` and `for-you.astro` are gone.
- `dist/docs/direct-democracy/index.html` and `dist/docs/why/index.html` do not exist (no twins).
- `dist/docs/book/direct-democracy/index.html`, `dist/book/principles/index.html` and `dist/docs/for-you/index.html` each contain a redirect (`http-equiv="refresh"` or "Redirecting").
- `astro.config.mjs` has a `redirects` table with ≥ 20 rows.
- `dist/book/egov-vison/index.html` does not exist after the production build (drafted out); in a `SITE_PREVIEW=true` build the eGov paths still resolve as redirects.
- `src/content/docs/book/principles.md` is deleted.

## Working rules (every E006 task)

- Repo root `C:/Users/zentala/code/zentala.eu`. Standards `DESIGN.md` and `src/AGENTS.md` are tracked and present in your worktree. Plan documents live under the main checkout's `.plan/` directory — read them by absolute path from `C:/Users/zentala/code/zentala.eu/.plan/`.
- First command in the worktree: `just setup`. Build: `just build`. Audit: `just audit`. Typecheck: `just typecheck`.
- Stage by explicit path (`git add <file>`), never `git add -A` / `git commit -a`; never stage the worktrees directory.
- Frontmatter `description` is 70–160 characters (schema-enforced).
- Dev-only features are gated on `IS_PREVIEW` (`src/lib/preview.ts`), never on `import.meta.env.DEV`.
- Preview servers: start on a free port, kill BY PORT when done; never kill by process name.
- Both themes must render (`.dark` class on `<html>`); check light and dark.
- Stay inside your write set; another task in this wave owns the other files. A change needed elsewhere → one entry in the main checkout's `.plan/BACKLOG.md` with path:line.
