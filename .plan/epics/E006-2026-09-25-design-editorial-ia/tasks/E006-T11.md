---
formatVersion: 1
type: feature
id: E006-T11
title: Chapters index from layer, "Chapters" label, eyebrow and ChapterFooter
epic: E006
status: completed
priority: high
effort: medium
agent: ts-dev
points: 5
model: standard
confidence: 5
summary: "Derive the /book index from the layer field instead of a hand-kept slug list, rename the reader-facing label to Chapters, add the layer·kind eyebrow on chapter pages, and generate every chapter footer from data."
created: 2026-09-25
wave: 4
completed_at: 2026-09-25
commits: [0dd613a]
verification_evidence: []
branch: feat/E006-T11
updated: 2026-09-25
---

# E006-T11: Chapters index from layer, "Chapters" label, eyebrow and ChapterFooter

Covers IA T03 (§2.1, §2.4), T06 (§6 rule 1) and §9 row 9 (`C:/Users/zentala/code/zentala.eu/.plan/INFORMATION-ARCHITECTURE.md`); DESIGN.md contract `ChapterFooter`. Wave 3 already landed `LAYERS`/`KINDS`/`layer`/`kind`/`order`/`concepts` in `src/content/config.ts`.

## Write set

- `src/pages/book/index.astro`, `src/pages/book/[...slug].astro`, `src/pages/book/[...slug].md.ts`
- `src/components/ChapterFooter.astro` (new), `src/components/Header.astro`, `src/components/Footer.astro`
- `src/pages/llms.txt.ts`, `src/pages/llms-full.txt.ts`

Do not touch chapter bodies (the 13 italic "*Part of the EU Reform Vision…*" paragraphs are deleted by the wave-5 editorial tasks), `src/lib/vision-content.ts` (E006-T19 — import its existing exports, do not rename them), `src/content/config.ts` (E006-T12), `astro.config.mjs` (E006-T12).

## Steps

1. §2.4: replace `GROUPS` with `LAYER_LABELS` + grouping by `entry.data.layer`, `Unsorted` fallback, `sortByOrder` (order, then title), a `kind` label on each card, and the one-line comment that `book` is a URL, not a label. Blurb at `book/index.astro:102` → one paragraph + link to `/vision` (§5 row 12).
2. §2.1 strings: "Chapters" in breadcrumbs, `<title>`, h1, `backLabel`, `Footer.astro` (visible, not `devOnly`), `Header.astro` nav, `llms.txt.ts` "## Chapters" listing chapters in `layer`/`order` sequence (§10 "reading-order manifest"). `jsonld.ts` `Book` node name may stay.
3. Chapter page: `<title>` = `<Chapter title> — EU Reform Vision`; an eyebrow above the h1 rendering `LAYER_LABELS[layer] · Kind` (e.g. `How we get there · Proposal`); omitted when `layer` is missing.
4. `ChapterFooter.astro` per §6 rule 1: previous/next by `order` inside the same `layer`; Up → `/vision#<layer>`; Related = up to three chapters sharing the most `concepts` (excluding prev/next, ties by `order`, section omitted when zero); Carry this → one fixed line linking `/manifesto` and `/support`. Wire it in `book/[...slug].astro` for every chapter.
5. `Footer.astro`: Sitemap link → `/site-map` (E006-T14 creates the page in this wave; the wave check verifies the link resolves after both merge); tagline → the frame-era one-liner from the existing `vision-content.ts` export (§5 row 13).
6. `just build`; Playwright screenshots (`chromium.launch({ channel: 'chrome' })`) of `/book/` and `/book/first-move/` in both themes to `.plan/reports/2026-09-25-e006/T11-*.png` in the main checkout.

## Unblocked (2026-09-25, second run)

The schema and the `layer`/`kind`/`order` frontmatter are now on main (`11af49b`, `25b82df`
via E006-T12): `grep -l "^layer:" src/content/docs/book/*` = 26. `concepts` arrays are being
populated by the re-scoped E006-T05 in the same wave — `ChapterFooter`'s Related section must
therefore tolerate empty `concepts` (section omitted), which the spec already requires.
The block note below is history.

## Blocked (2026-09-25, first run, ts-dev)

The task header claims "Wave 3 already landed `LAYERS`/`KINDS`/`layer`/`kind`/`order`/`concepts`
in `src/content/config.ts`." Verified false at dispatch time: `src/content/config.ts` has no
`LAYERS`, `KINDS`, `layer`, `kind`, `order` or `concepts` fields (only `title`, `description`,
`tags`, `author`, `customSlug`, `draft`, `date`, `updated`), and zero files under
`src/content/docs/book/` carry `layer:`/`kind:`/`order:`/`concepts:` frontmatter
(`grep -l "^layer:" src/content/docs/book/*.{md,mdx}` = 0 matches). `E006-T05` — the task that
owns `src/content/config.ts` and the frontmatter sweep — is still `status: pending` (agent
`editor`, wave 3, 8 pts); no `feat/E006-T05` branch or commit exists anywhere in `git log --all`.

T11's write set explicitly excludes `src/content/config.ts` and chapter frontmatter, so this
cannot be worked around inside T11's scope: every acceptance check (`grep`-verified — "does not
contain Unsorted", "How we get there · Playbook" eyebrow, `layer`/`order`-based prev/next,
`concepts`-based Related) requires data that does not exist on disk yet. Implementing the Astro
code against a schema/data set that isn't there would be unverifiable and would need rewriting
once T05's actual field names/shapes land. No worktree was created; no files were changed.

**Unblocks when:** `E006-T05` merges to `main` with `LAYERS`/`KINDS`/`layer`/`kind`/`order`/
`concepts` in `src/content/config.ts` and frontmatter set on the 19 chapters in IA §2.5.

## Acceptance

- `just build` exits 0.
- `grep -c "Vision Book" dist/book/index.html dist/book/first-move/index.html src/components/Footer.astro` = 0 on each.
- `dist/book/index.html` contains "The frame", "How we get there" and does not contain "Unsorted" (every published chapter is classified).
- `dist/book/first-move/index.html` contains the eyebrow text "How we get there · Playbook", a previous link to `/book/political-path/`, an Up link to `/vision#path`, and the "Carry this" line.
- `dist/llms.txt` contains "## Chapters" and lists `designing-our-retirement` before `redesigning-the-state`.
- `grep -n "GROUPS" src/pages/book/index.astro` returns nothing.

## Working rules (every E006 task)

- Repo root `C:/Users/zentala/code/zentala.eu`. Standards `DESIGN.md` and `src/AGENTS.md` are tracked and present in your worktree. Plan documents live under the main checkout's `.plan/` directory — read them by absolute path from `C:/Users/zentala/code/zentala.eu/.plan/`.
- First command in the worktree: `just setup`. Build: `just build`. Audit: `just audit`. Typecheck: `just typecheck`.
- Stage by explicit path (`git add <file>`), never `git add -A` / `git commit -a`; never stage the worktrees directory.
- Dev-only features are gated on `IS_PREVIEW` (`src/lib/preview.ts`), never on `import.meta.env.DEV`.
- Preview servers: start on a free port, kill BY PORT when done; never kill by process name.
- Colours, sizes, radii, fonts come from tokens; no hard-coded values in components.
- Both themes must render (`.dark` class on `<html>`); check light and dark.
- Stay inside your write set; another task in this wave owns the other files. A change needed elsewhere → one entry in the main checkout's `.plan/BACKLOG.md` with path:line.
