---
formatVersion: 1
type: improvement
id: E006-T19
title: One argument, one home — the top-level pages
epic: E006
status: completed
priority: high
effort: large
agent: editor
points: 8
model: high-capability
confidence: 4
summary: "Turn every restated argument on /why, /manifesto, /principles and the shared frame copy into a pointer to its canonical chapter, restructure /why as an objections page, and move /principles onto the article layout."
created: 2026-09-25
wave: 5
completed_at: 2026-09-25
commits: [40e5618]
verification_evidence: []
branch: feat/E006-T19
updated: 2026-09-25
---

# E006-T19: One argument, one home — the top-level pages

Covers IA T13 (§5 rows for top-level pages), §2.5 rows for `why`/`manifesto`, `DESIGN.md` Migration step 3 for `/principles` only, and the R4 structural edit of `/why` (`C:/Users/zentala/code/zentala.eu/.plan/INFORMATION-ARCHITECTURE.md`, `C:/Users/zentala/code/zentala.eu/.plan/reports/2026-09-25-e006/R4-editorial-audit.md`). The chapter groups (T15–T18) run in this wave in parallel and own the chapter files; their canonical anchors are the ones IA §5 names — link to those anchors even before the chapters merge.

## Write set

- `src/content/docs/why.mdx`, `src/content/docs/manifesto.mdx`
- `src/pages/principles.astro` (content and the move onto `ArticleLayout`)
- `src/lib/vision-content.ts` (edit strings only; do not rename or remove exports — `Footer.astro` and the homepage import them)
- `src/pages/index.astro` (copy only, where §5 demands)

Do not touch chapters (T15–T18), `why.astro`/`manifesto.astro` shells (wave 4 moved them onto `ArticleLayout`), `Footer.astro`, `book/index.astro`.

## Steps

1. Frontmatter: `why.mdx` gets `kind: objections`, `manifesto.mdx` gets `kind: manifesto`, both `updated: 2026-09-25`, `concepts` for the governed terms they use; `description` 70–160.
2. `/why` as an objections page (R4 structural edit): each objection = `Objection` inside an `ObjectionList`, answer ≤ 5 sentences, ending with a link to the canonical chapter. §5 rows: 2 (`why:120-131` → 3–5 sentence answer + link to `/book/cheap-is-wealth`), 4 (`:59-76` → answer + link to `/book/education-and-migration`), 5 (`:215-237` → answer + link to `/book/designing-our-retirement`), 6 (`:94-95` delete the repeated joke), 7 (`:283-287` → one line linking `/manifesto`), 15 (`:189-202` → link to the political-path table). The four-step frame at `:27-37` stays a pointer.
3. `/manifesto`: §5 rows 1 (`:214-220` → one sentence per instrument + `<T slug="citizens-initiative">` / `<T slug="optional-referendum">`), 7 (`:249-257` is the canonical "you are the condition of it" — keep, tighten), 9 (`:93-94` keep the personal sentence only if it does work there), 10 (`:138-152` → 2 sentences + link to `/book/european-science`); the method-value section ("look for what everyone can agree on") stays — it is dictated; use `ManifestoDemand`'s successor `Callout` and `Lead` per DESIGN.
4. `/principles`: §5 row 2 (`:147-171` → the four rules as a list + links to `/book/cheap-is-wealth` and `/book/who-it-earns-for#it-has-to-earn-for-society-not-only-for-capital`), row 11 (`:138-145` stays a pointer); render through `ArticleLayout` (66ch) with `Section`/`Lead` — the 52rem frame goes.
5. `vision-content.ts`: row 7 — reword `callToAction` so it is not the manifesto's sentence; row 13 — confirm the frame-era one-liner export is the one `Footer.astro` now uses (do not rename). `index.astro`: only the copy §5 requires.
6. Provenance: manifesto and why carry no ProvenanceNote (kind manifesto/objections), but every first-person claim must still trace to a dump; note the mapping in the report.
7. `just build`; `just audit` (its canonical-home and `/docs/` checks must pass for these pages); `node scripts/design-lint.mjs`.

## Acceptance

- `just build` exits 0; `just audit` reports 0 `/docs/` links and 0 redirect-table links on `/why`, `/manifesto`, `/principles`.
- `grep -c "nail" src/content/docs/why.mdx` = 0 (row 6).
- `why.mdx` uses `ObjectionList`/`Objection`; every `Objection` answer contains a link to `/book/` or `/manifesto`.
- `manifesto.mdx` mentions the initiative and the referendum with at most one sentence each (the report quotes them) and links `/book/direct-democracy#the-words-in-one-place`.
- `dist/principles/index.html` contains the `ArticleGrid` markup and links both canonical homes from row 2.
- `grep -n "export" src/lib/vision-content.ts` is unchanged in names (diff shows string edits only).
- `node scripts/design-lint.mjs` reports 0 for the consecutive-paragraphs rule on `why.mdx`, `manifesto.mdx`.

## Defaults decided on 2026-09-25 (Paweł's go on `/implement`; he can overrule later)

- Q1 communism: framing lives in `who-it-earns-for`; manifesto and why link, never restate.
- Q3 thresholds: 2 % / 1 %, stated in `direct-democracy` only.
- Q5 register: the image stays, the vulgarity goes, the strength of the verdict stays; no softening clause.
- Rights floor: non-derogable ECHR core only ("less rather than more").
- "Economic euthanasia" passage stays unpublished (it is not restored on `/why`).
- The manifesto's method value ("find what we can all agree on, work it out deeply, present it") is dictated and stays as a named value.

## Working rules (every E006 task)

- Repo root `C:/Users/zentala/code/zentala.eu`. Standards `DESIGN.md` and `src/AGENTS.md` are tracked and present in your worktree. Plan documents live under the main checkout's `.plan/` directory — read them by absolute path from `C:/Users/zentala/code/zentala.eu/.plan/`.
- First command in the worktree: `just setup`. Build: `just build`. Audit: `just audit`.
- Stage by explicit path (`git add <file>`), never `git add -A` / `git commit -a`; never stage the worktrees directory.
- Frontmatter `description` is 70–160 characters (schema-enforced).
- Preview servers: start on a free port, kill BY PORT when done; never kill by process name.
- Stay inside your write set; another task in this wave owns the other files. A change needed elsewhere → one entry in the main checkout's `.plan/BACKLOG.md` with path:line.
