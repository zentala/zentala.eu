---
formatVersion: 1
type: feature
id: E006-T15
title: The floor chapter — one working language
epic: E006
status: pending
priority: high
effort: medium
agent: editor
points: 5
model: high-capability
confidence: 4
summary: "Write the missing argument chapter for the language layer from the frame dump and the sidecar, fold the old language stub into it, and split the standards spec so the floor has an argument to land on."
created: 2026-09-25
wave: 5
completed_at: null
commits: []
verification_evidence: []
branch: feat/E006-T15
---

# E006-T15: The floor chapter — one working language

Covers IA T12 and §10 row 1, §2.5 language rows (`C:/Users/zentala/code/zentala.eu/.plan/INFORMATION-ARCHITECTURE.md`). Waves 3–4 landed the schema, the components (Objection, EvidenceBox, KeyFigure, ProvenanceNote, MeansForYou, SideNote, ComparisonTable, Playbook), `TermRef`, `ChapterFooter` and the `/book/language` → `/book/one-working-language` redirect.

## Write set

- `src/content/docs/book/one-working-language.mdx` (new)
- `src/content/docs/book/language.md` (merge what is dictated into the new chapter, then delete)
- `src/content/docs/book/language-integration-administrative-implementation.mdx` (R4: invented timeline — remove the invented material; if nothing dictated survives, set `draft: true` and add one `.plan/BACKLOG.md` line in the main checkout proposing deletion)
- `src/content/docs/book/european-english.mdx` (the argument moves out to the new chapter; the spec and appendix stay), `european-english.sidecar.mdx` (read first; move nothing out of it that is not dictated)

Do not touch the held stubs `economic-growth-language-unity.mdx`, `media-cultural-integration.mdx`, nor any other chapter (T16–T18) or top-level page (T19).

## Sources (read in full before writing)

- `C:/Users/zentala/code/zentala.eu/.plan/vision/aging-society-frame.md` lines 44–70 (the floor argument in his words: English as the bottleneck of Europe's information economy; no European political consciousness without a shared language; a shared interest keeps us from turning on each other).
- `language.md` lines 16 and 22 (his voice), the whole file for anything else dictated.
- `european-english.sidecar.mdx` (2,707 words — it may already hold the argument).
- R4 audit (`C:/Users/zentala/code/zentala.eu/.plan/reports/2026-09-25-e006/R4-editorial-audit.md`), question Q6 and the language rows.
- Paweł's stated position on Central Europe and English (2026-09-22 chat, recorded in the E006 PLAN and the R4 audit): Central Europe first, English in administration, including the military — the military point is his; the *arguments* for it that agents researched are not.

## Steps

1. Audit every candidate sentence: dictated / researched / proposed. First person only for dictated.
2. Write `one-working-language.mdx`: frontmatter `layer: language`, `kind: argument`, `order: 1`, `concepts: [the-three-layers, the-frame]` (+ any glossary slug the text uses), `provenance: dictated`, `description` 70–160 chars, `draft: true` until the checklist passes. Structure per `src/AGENTS.md` §6 and the DESIGN grammar: lead; the argument in his words; an EvidenceBox for any sourced fact (source named, no invented figure); an ObjectionList with the standard objections (Irish/Maltese precedent, French resistance, cost, "it is already the working language") answered in ≤ 5 sentences each; a MeansForYou closer; a ProvenanceNote listing Source / Dictated (dump:lines) / Added / Cut / Open. Never more than 4 paragraphs in a row without a structural element.
3. Fold `language.md`: dictated sentences move in verbatim or lightly edited; the rest is cut and listed under "Cut" in the ProvenanceNote; delete the file.
4. `european-english.mdx`: remove the argument passages that now live in the floor chapter, leave a 2–3 sentence pointer + link; the spec, standards and appendix stay; check its frontmatter still fits (`kind: proposal`, `order: 2`).
5. Run the §11 checklist; flip `draft: false` only if it passes; `just build`; `node scripts/design-lint.mjs` on the new file.

## Acceptance

- `just build` exits 0; `dist/book/one-working-language/index.html` exists; `dist/book/language/index.html` is a redirect stub.
- The new chapter contains at least ObjectionList, EvidenceBox and ProvenanceNote (grep the component names in the source).
- Every first-person sentence in the new chapter maps to a dump line listed in the ProvenanceNote (the report lists the mapping).
- No numeric claim without a named source (`grep -n "[0-9]\{2,\}" src/content/docs/book/one-working-language.mdx` → each hit is inside an EvidenceBox or a KeyFigure with `source`).
- `description` length 70–160; `src/content/docs/book/language.md` deleted.
- Report lists ASK-n questions with dump:line references.

## Defaults decided on 2026-09-25 (Paweł's go on `/implement`; he can overrule later)

- Q1 communism: keep his own framing from the surplus dump, once, in `who-it-earns-for`; other chapters link.
- Q3 thresholds: 2 % / 1 % as dictated, stated once in `direct-democracy` "The words, in one place"; other chapters cite by link.
- Q4/Q6: the military argument for English — the *position* is his (chat 2026-09-22); the supporting arguments are agent research and are written as offered, never first person.
- Q5 register: the image stays, the vulgarity goes, the strength of the verdict stays; no softening clause.
- Q9: eGov chapters merge into `european-egovernment`; where they contradict `the-total-state` (dictated), the-total-state wins.
- Rights floor: non-derogable ECHR core only ("less rather than more").
- "Economic euthanasia" passage and the personal emotional-isolation passage stay unpublished.
- Dividend vs services: school meals and public services as dictated; a citizens' dividend is an offered option, not his demand.

## Working rules (every E006 task)

- Repo root `C:/Users/zentala/code/zentala.eu`. Standards `DESIGN.md` and `src/AGENTS.md` are tracked and present in your worktree. Plan documents live under the main checkout's `.plan/` directory — read them by absolute path from `C:/Users/zentala/code/zentala.eu/.plan/`.
- First command in the worktree: `just setup`. Build: `just build`. Audit: `just audit`.
- Stage by explicit path (`git add <file>`), never `git add -A` / `git commit -a`; never stage the worktrees directory.
- Frontmatter `description` is 70–160 characters (schema-enforced). Components require `.mdx`; the slug never changes.
- Preview servers: start on a free port, kill BY PORT when done; never kill by process name.
- Stay inside your write set; another task in this wave owns the other files. A change needed elsewhere → one entry in the main checkout's `.plan/BACKLOG.md` with path:line.
