---
formatVersion: 1
type: improvement
id: E006-T16
title: Chapter migration, group 1 — the worst walls of prose
epic: E006
status: pending
priority: high
effort: large
agent: editor
points: 8
model: high-capability
confidence: 4
summary: "Edit choosing-people, the-total-state, new-cities, cheap-is-wealth and the-shorter-week to the editorial standard and the DESIGN grammar: provenance audited against the dumps, agent research re-voiced, invented figures out, components in, generic footers deleted."
created: 2026-09-25
wave: 5
completed_at: null
commits: []
verification_evidence: []
branch: feat/E006-T16
---

# E006-T16: Chapter migration, group 1 — the worst walls of prose

Covers `DESIGN.md` Migration step 12 (first five, R2 B9 order) and IA §5 / §6 rule 1 for these files (`C:/Users/zentala/code/zentala.eu/.plan/INFORMATION-ARCHITECTURE.md`). Waves 3–4 landed the components (Objection, ObjectionList, MeansForYou, ComparisonTable, Playbook, KeyFigure, SideNote, ProvenanceNote, PullQuote, SourceList, Figure, EvidenceBox, Callout, Timeline), `TermRef` (`<T slug="…">`), the generated `ChapterFooter`, and the frontmatter fields.

## Write set (these files only, rename `.md` → `.mdx` when components are used)

- `src/content/docs/book/choosing-people.md`
- `src/content/docs/book/the-total-state.md`
- `src/content/docs/book/new-cities.md`
- `src/content/docs/book/cheap-is-wealth.md`
- `src/content/docs/book/the-shorter-week.md`

## Sources

- Dumps in `C:/Users/zentala/code/zentala.eu/.plan/vision/` — `CHAPTER-IDEAS.md` there maps chapters to dumps; for the rest `grep -il` the chapter's subject (choosing people → `2026-09-25-open-door-and-choosing-people-dump.md`; total state → `2026-09-25-direct-democracy-dump.md`; new cities, cheap is wealth, shorter week → the dated dumps with those subjects, e.g. `2026-09-25-education-migration-workweek-dump.md` for the week).
- R4 audit (`C:/Users/zentala/code/zentala.eu/.plan/reports/2026-09-25-e006/R4-editorial-audit.md`): the per-chapter rows for these five (invented figures, attribution defects, the 7 contradictions).
- IA §5 rows touching these files: row 1 (`the-total-state:101-113` → pointer to the glossary anchor), row 2 (`cheap-is-wealth` is a canonical home — keep, tighten), row 17 (closers).

## Editorial procedure (per chapter)

1. Audit every sentence against the dump (`src/AGENTS.md` §4 step 10): dictated / researched / proposed. First person only for dictated; researched and proposed content re-voiced as offered ("An option I would put on the table", "The evidence I found says…") and gathered under a ProvenanceNote (Source / Dictated with dump:lines / Added / Cut / Open).
2. Structure per `src/AGENTS.md` §6 and the DESIGN grammar: lead; KeyFigure where a number carries the argument; ComparisonTable for alternatives; Playbook for steps; ObjectionList for the standard counter-arguments; EvidenceBox for sourced facts; SideNote for asides; PullQuote at most once; MeansForYou closer specific to the chapter (the generic "carry the argument" sentences go — the generated footer carries them). Never more than 4 paragraphs in a row without a structural element.
3. Invented figures (R4 list) are removed or sourced; contradictions resolved per the defaults below.
4. Apply the IA §5 rows for these files: a non-canonical restatement becomes ≤ 3 sentences + link to the canonical anchor; first mention of a governed term uses `<T slug="…">` where the auto-linker would not catch it.
5. Delete the italic "*Part of the EU Reform Vision…*" closing paragraph.
6. Frontmatter: keep wave-3 `layer/kind/order/concepts`; add `provenance` (`dictated` when the spine is his, `researched` otherwise), `updated: 2026-09-25`; `description` 70–160; `.mdx` when components are used.
7. `src/AGENTS.md` §11 checklist; `just build`; `node scripts/design-lint.mjs` (the 4-paragraph rule must report 0 for these files).
8. Report per chapter: words before/after, components used, cuts, ASK-n questions with dump:line.

## Acceptance

- `just build` exits 0; all five chapters still resolve at their old slugs.
- Each of the five files imports and uses ≥ 2 of the content components and contains a `ProvenanceNote`.
- `grep -l "Part of the EU Reform Vision" src/content/docs/book/*` does not list these five.
- `node scripts/design-lint.mjs` reports 0 for the consecutive-paragraphs rule on these files.
- Every figure that R4 flagged as invented in these chapters is gone or carries a source (the report lists each by R4 row).
- `description` 70–160 on each; `provenance` set on each.

## Defaults decided on 2026-09-25 (Paweł's go on `/implement`; he can overrule later)

- Q1 communism: keep his own framing from the surplus dump, once, in `who-it-earns-for` (T18); `cheap-is-wealth` and `the-total-state` link, they do not restate.
- Q3 thresholds: 2 % / 1 % as dictated, stated once in `direct-democracy` "The words, in one place"; other chapters cite by link.
- Q5 register: the image stays, the vulgarity goes, the strength of the verdict stays; no softening clause.
- Q9: eGov chapters merge into `european-egovernment`; where they contradict `the-total-state` (dictated), the-total-state wins — so do not soften the-total-state.
- Rights floor: non-derogable ECHR core only ("less rather than more").
- "Economic euthanasia" passage and the personal emotional-isolation passage stay unpublished.
- Dividend vs services: school meals and public services as dictated; a citizens' dividend is an offered option, not his demand.
- Sortition, functional representation, the vote-by-portfolio mechanics in `choosing-people`: his demand is the procedure that admits the competent; prior art (open lists, confirmation hearings, EP hearings) is research → offered.

## Working rules (every E006 task)

- Repo root `C:/Users/zentala/code/zentala.eu`. Standards `DESIGN.md` and `src/AGENTS.md` are tracked and present in your worktree. Plan documents live under the main checkout's `.plan/` directory — read them by absolute path from `C:/Users/zentala/code/zentala.eu/.plan/`.
- First command in the worktree: `just setup`. Build: `just build`. Audit: `just audit`.
- Stage by explicit path (`git add <file>`), never `git add -A` / `git commit -a`; never stage the worktrees directory.
- Frontmatter `description` is 70–160 characters (schema-enforced). Components require `.mdx`; the slug never changes.
- Preview servers: start on a free port, kill BY PORT when done; never kill by process name.
- Stay inside your write set; another task in this wave owns the other files. A change needed elsewhere → one entry in the main checkout's `.plan/BACKLOG.md` with path:line.
