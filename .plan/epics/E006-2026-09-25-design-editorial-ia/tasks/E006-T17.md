---
formatVersion: 1
type: improvement
id: E006-T17
title: Chapter migration, group 2 — the frame and the democratic instruments
epic: E006
status: completed
priority: high
effort: large
agent: editor
points: 8
model: high-capability
confidence: 4
summary: "Edit designing-our-retirement, robotic-reindustrialisation, political-path, direct-democracy and safeguards to the editorial standard: provenance audited, safeguards re-voiced as a proposal, the thresholds stated once, duplicated arguments turned into pointers, components in."
created: 2026-09-25
wave: 5
completed_at: 2026-09-25
commits: [d1d431b]
verification_evidence: []
branch: feat/E006-T17
updated: 2026-09-25
---

# E006-T17: Chapter migration, group 2 — the frame and the democratic instruments

Covers `DESIGN.md` Migration step 12 for these five and IA §4, §5, §6 rule 1 for these files (`C:/Users/zentala/code/zentala.eu/.plan/INFORMATION-ARCHITECTURE.md`). Waves 3–4 landed the components, `TermRef` (`<T slug="…">`), the generated `ChapterFooter` and the frontmatter fields.

## Write set (these files only, rename `.md` → `.mdx` when components are used)

- `src/content/docs/book/designing-our-retirement.md`
- `src/content/docs/book/robotic-reindustrialisation.md`
- `src/content/docs/book/political-path.md`
- `src/content/docs/book/direct-democracy.md`
- `src/content/docs/book/safeguards.md`

## Sources

- Dumps in `C:/Users/zentala/code/zentala.eu/.plan/vision/`: `aging-society-frame.md` (retirement, robotics), `2026-09-25-direct-democracy-dump.md`, `2026-09-25-answers-and-safeguards-dump.md`, the political-path dictation recorded in the E006 PLAN / R4 audit; `CHAPTER-IDEAS.md` maps the rest.
- R4 audit (`C:/Users/zentala/code/zentala.eu/.plan/reports/2026-09-25-e006/R4-editorial-audit.md`): rows for these five — especially the attribution defect in `safeguards` (Q2: agent research in his first person) and the two threshold figures.
- IA §4 (where the glossary text comes from): `direct-democracy:72-82` shortened to the sentences the glossary quotes; `:310-346` stays as the home of four glossary entries and the numbers are stated there only; `safeguards:27-50` keeps the validity checks and the double-majority worked example, replaces the re-derived thresholds with one link.
- IA §5 rows: 1 (`political-path:66-73` → pointer), 2 (`designing-our-retirement:146-154`, `robotic-reindustrialisation:103-120` → 2 sentences + link), 3 (`designing-our-retirement:93-109` → the targets table stays, the care argument goes to robotics), 4 (`designing-our-retirement:64-78` → 3 sentences + link), 5 (retirement is canonical for "we already pay"), 6, 7 (`:168-169` one clause + link), 8 (`political-path:56-64` → 2 sentences + link), 11, 14, 15 (`political-path:145-157` becomes one table — ComparisonTable), 17.

## Editorial procedure (per chapter)

1. Audit every sentence against the dump (`src/AGENTS.md` §4 step 10): dictated / researched / proposed. First person only for dictated; research re-voiced as offered and gathered under a ProvenanceNote (Source / Dictated with dump:lines / Added / Cut / Open). `safeguards` becomes explicitly a proposal built on Swiss practice, with the verified Fedlex facts in EvidenceBoxes and his own choices (2 % / 1 %, paper fallback, technocrats prepare / citizens approve) in first person.
2. Structure per `src/AGENTS.md` §6 and the DESIGN grammar (lead, KeyFigure, ComparisonTable, Playbook, ObjectionList, EvidenceBox, SideNote, PullQuote ≤ 1, MeansForYou specific to the chapter). Never more than 4 paragraphs in a row without a structural element. `direct-democracy` "The words, in one place" stays a section with `Callout definition` anchors so the glossary `href`s resolve.
3. Invented figures (R4) removed or sourced; the 7 contradictions resolved where they fall here (thresholds → one figure; longevity dissolving the premise → the frame states it as his bet, with the objection answered).
4. Apply the §5 rows above; `<T slug="…">` on first mentions the auto-linker would miss.
5. Delete the italic "*Part of the EU Reform Vision…*" closing paragraph.
6. Frontmatter: keep `layer/kind/order/concepts`; add `provenance`, `updated: 2026-09-25`; `description` 70–160; `.mdx` when components are used.
7. `src/AGENTS.md` §11 checklist; `just build`; `node scripts/design-lint.mjs`.
8. Report per chapter: words before/after, components used, cuts, ASK-n questions with dump:line.

## Acceptance

- `just build` exits 0; the five slugs still resolve; the anchors `#the-words-in-one-place` (direct-democracy), `#it-is-also-the-democratic-answer-to-the-anger` (direct-democracy) and `#the-safeguards-one-by-one` (safeguards) exist in the built HTML (glossary `href`s depend on them; `just audit` checks).
- Each file imports and uses ≥ 2 content components and contains a `ProvenanceNote`.
- `grep -c "580,000\|580 000\|7,180,000" src/content/docs/book/*` — the scaled figures appear in exactly one chapter (`safeguards` arithmetic or `direct-democracy`, not both) and are derived from the 2 % / 1 % rule.
- `grep -l "Part of the EU Reform Vision" src/content/docs/book/*` does not list these five.
- `node scripts/design-lint.mjs` reports 0 for the consecutive-paragraphs rule on these files.
- `political-path` contains a ComparisonTable for the five legal questions.

## Defaults decided on 2026-09-25 (Paweł's go on `/implement`; he can overrule later)

- Q1 communism: his own framing, once, in `who-it-earns-for` (T18); these chapters link.
- Q3 thresholds: 2 % / 1 % as dictated, stated once in `direct-democracy` "The words, in one place"; `safeguards` and `first-move` cite by link.
- Q2 safeguards: the Swiss mapping is a proposal built on research — re-voiced; his choices stay first person.
- Q5 register: the image stays, the vulgarity goes, the strength of the verdict stays.
- Rights floor: non-derogable ECHR core only ("less rather than more").
- "Economic euthanasia" and the personal emotional-isolation passage stay unpublished.
- Energy and defence: one section each at most, his positions as recorded (nuclear on the table and a citizens' vote; a common army needs citizen power first) — not new chapters.
- Dividend vs services: services as dictated; a citizens' dividend is an offered option.

## Working rules (every E006 task)

- Repo root `C:/Users/zentala/code/zentala.eu`. Standards `DESIGN.md` and `src/AGENTS.md` are tracked and present in your worktree. Plan documents live under the main checkout's `.plan/` directory — read them by absolute path from `C:/Users/zentala/code/zentala.eu/.plan/`.
- First command in the worktree: `just setup`. Build: `just build`. Audit: `just audit`.
- Stage by explicit path (`git add <file>`), never `git add -A` / `git commit -a`; never stage the worktrees directory.
- Frontmatter `description` is 70–160 characters (schema-enforced). Components require `.mdx`; the slug never changes.
- Preview servers: start on a free port, kill BY PORT when done; never kill by process name.
- Stay inside your write set; another task in this wave owns the other files. A change needed elsewhere → one entry in the main checkout's `.plan/BACKLOG.md` with path:line.
