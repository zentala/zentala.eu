---
formatVersion: 1
type: improvement
id: E006-T13
title: Long pages onto ArticleLayout, chart figure, chrome cleanup
epic: E006
status: completed
priority: medium
effort: medium
agent: ts-dev
points: 5
model: standard
confidence: 4
summary: "Move /why and /manifesto onto the shared ArticleLayout, adopt the statistics chart under Figure with token colours, retire the last unused home components, list each layer's chapters on /vision, and replace the remaining hard-coded sizes with tokens."
created: 2026-09-25
wave: 4
completed_at: 2026-09-25
commits: [ce6bd88]
verification_evidence: []
branch: feat/E006-T13
updated: 2026-09-25
---

# E006-T13: Long pages onto ArticleLayout, chart figure, chrome cleanup

Covers `DESIGN.md` Migration steps 3 (except `/principles`, which E006-T19 owns), 10 and 13, the "replaces" column of step 1, and IA §6 rule 6 (layer pages link down). Wave 3 landed the tokens, `ArticleGrid`, the renamed components and `Figure`.

## Write set

- `src/pages/why.astro`, `src/pages/manifesto.astro`, `src/pages/home-classic.astro`, `src/pages/vision/index.astro`, `src/pages/ui/index.astro`
- `src/components/why/WhyAnswer.astro` (delete, and the folder), `src/components/Figure.astro`, `src/components/EUStatisticsChart.astro`, `src/components/InteractiveSection.astro` / `EuropeMap.astro` / `Testimonials.astro` (delete), `src/components/DisplayTitle.astro`, `src/components/Hero.astro`, `src/components/Newsletter.astro`, plus any component still carrying `0.8125rem`, `0.9375rem`, `1.7` or `0.1em` literals (grep; E006-T11 owns `Header`, `Footer`, `ChapterFooter` — leave those to a backlog note if they match)

Do not touch `src/content/docs/why.mdx` / `manifesto.mdx` bodies (E006-T19), `src/pages/index.astro` (E006-T19), `src/pages/principles.astro` (E006-T19), `src/lib/vision-content.ts` (E006-T19).

## Steps

1. Step 3: `why.astro` and `manifesto.astro` render through `ArticleLayout` (66ch measure, `ArticleGrid`); the 68rem/52rem frames and `WhyAnswer` go.
2. Step 10: `Figure` adopts `EUStatisticsChart` with series colours from tokens (36 hard-coded colours go); retire `InteractiveSection`, `EuropeMap`, `Testimonials`, updating `home-classic.astro` so the preview-only backup still builds.
3. Step 13: `DisplayTitle` is already a dynamic tag (E004-T11, merge b424eb1) — only its margins from the Typography tokens remain; `Hero` shadow from the elevation token; `Newsletter` loses `outline-none` (focus ring from the accent token). Also update `src/pages/ui/index.astro` examples for anything you retire (it imports `Testimonials`, `InteractiveSection`, `EuropeMap`, `EUStatisticsChart`).
4. Step 1 "replaces": every remaining `0.8125rem` / `0.9375rem` / `1.7` / `0.1em` literal in components → the wave-3 tokens.
5. §6 rule 6: under each `/vision` layer anchor (`#language`, `#digital`, `#robotics`; the frame's chapters under the intro) list that layer's published chapters from the collection by `order`, title + `kind` label, linking `/book/<slug>`.
6. `just build`; crawl `/why`, `/manifesto`, `/vision`, `/home-classic` in both themes with Playwright (`chromium.launch({ channel: 'chrome' })`); screenshots to `.plan/reports/2026-09-25-e006/T13-*.png` in the main checkout.

## Acceptance

- `just build` exits 0.
- `grep -rn "0\.8125rem\|0\.9375rem\|0\.1em" src/components src/layouts src/pages` returns nothing outside `Header.astro`/`Footer.astro`/`ChapterFooter.astro` (owned elsewhere this wave).
- `grep -c "#[0-9a-fA-F]\{6\}" src/components/EUStatisticsChart.astro` = 0.
- `src/components/why/`, `InteractiveSection.astro`, `EuropeMap.astro`, `Testimonials.astro` are gone; `dist/home-classic/index.html` exists in a `SITE_PREVIEW=true` build.
- `dist/why/index.html` and `dist/manifesto/index.html` contain the `ArticleGrid` markup (class or data attribute the component emits).
- `dist/vision/index.html` lists `/book/the-total-state/` under `id="digital"` and `/book/robotic-reindustrialisation/` under `id="robotics"`.
- 4 pages × 2 themes crawled, 0 console errors.

## Working rules (every E006 task)

- Repo root `C:/Users/zentala/code/zentala.eu`. Standards `DESIGN.md` and `src/AGENTS.md` are tracked and present in your worktree. Plan documents live under the main checkout's `.plan/` directory — read them by absolute path from `C:/Users/zentala/code/zentala.eu/.plan/`.
- First command in the worktree: `just setup`. Build: `just build`. Audit: `just audit`. Typecheck: `just typecheck`.
- Stage by explicit path (`git add <file>`), never `git add -A` / `git commit -a`; never stage the worktrees directory.
- Dev-only features are gated on `IS_PREVIEW` (`src/lib/preview.ts`), never on `import.meta.env.DEV`.
- Preview servers: start on a free port, kill BY PORT when done; never kill by process name.
- Colours, sizes, radii, fonts come from tokens; no hard-coded values in components.
- Both themes must render (`.dark` class on `<html>`); check light and dark.
- Stay inside your write set; another task in this wave owns the other files. A change needed elsewhere → one entry in the main checkout's `.plan/BACKLOG.md` with path:line.
