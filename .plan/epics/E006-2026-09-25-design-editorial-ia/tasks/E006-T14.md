---
formatVersion: 1
type: feature
id: E006-T14
title: Site map page and the shape-checking audit
epic: E006
status: pending
priority: medium
effort: large
agent: ts-dev
points: 8
model: standard
confidence: 4
summary: "A /site-map page with a static multi-column list and a build-time SVG link graph as its companion, and content-audit checks for the information architecture rules (two clicks, layer/kind, glossary anchors, canonical homes, redirect-table links)."
created: 2026-09-25
wave: 4
completed_at: null
commits: []
verification_evidence: []
branch: feat/E006-T14
---

# E006-T14: Site map page and the shape-checking audit

Covers IA T10 (§8) and T11 (§10 last row, §6 rules 2–4, §9 closing paragraph) — `C:/Users/zentala/code/zentala.eu/.plan/INFORMATION-ARCHITECTURE.md`; DESIGN.md contract `SiteMap`. `scripts/link-graph.mjs` and `src/data/link-graph.json` exist since wave 3.

## Write set

- `src/pages/site-map.astro` (new) and any helper under `src/lib/site-map.ts`
- `scripts/content-audit.mjs`
- `scripts/link-graph.mjs` only if the JSON needs one more field for the SVG (say so in the report)

Do not touch `Footer.astro` (E006-T11 changes the Sitemap link to `/site-map` this wave), `astro.config.mjs` (E006-T12), `src/content/config.ts` (E006-T12).

## Steps

1. §8.1: page title and h1 "Site Map"; columns Start here / The frame / Floor / Middle / Ceiling / How we get there / Reference exactly as the table says; each line title + small `kind` label; drafts filtered like `/book`; "Unsorted" shown when non-empty; columns collapse to one at phone width.
2. §8.2: inline SVG produced at build from `src/data/link-graph.json`: columns = layers in frame order + Pages + Reference; nodes ordered by `order`; node fill by layer colour (DESIGN.md tokens), border style by `kind` (solid / dashed / double), size by inbound count in three steps; curved edges with arrowheads; hover/focus highlight through CSS `:has()` only; each node an `<a>`. Preview builds (`IS_PREVIEW`) draw dead links and orphans in the warning colour with a legend line. Deterministic layout.
3. Accessibility fallback: `<figure aria-hidden="true">` + `<figcaption>`; a `<details>` "Connections as text" listing "links to: …" per chapter from the same JSON.
4. `scripts/content-audit.mjs` gains, with counts and `path:line` in the report: (a) two-click depth from `/` for every published page; (b) `layer` and `kind` present on every published `book/*` entry, `kind` on `why`/`manifesto`; (c) every glossary `href` resolves to an existing id in the built HTML; (d) no `/docs/` link in any chapter, `/why`, `/manifesto`; (e) no built HTML links a path in the `astro.config.mjs` redirect table; (f) a chapter that declares a concept links its canonical `href` (glossary) somewhere in its body or footer. Zero processed files = failure.
5. `just build`; `just audit`; Playwright screenshots (`chromium.launch({ channel: 'chrome' })`) of `/site-map/` in both themes to `.plan/reports/2026-09-25-e006/T14-*.png` in the main checkout.

## Acceptance

- `just build` exits 0; `dist/site-map/index.html` contains the seven column headings, an inline `<svg>`, and a `<details>` element.
- `grep -c "<a " dist/site-map/index.html` ≥ 30.
- `just audit` prints the six new checks with counts; deleting `src/data/link-graph.json` and running the audit alone reports the missing graph as a failure, not a pass.
- Screenshots exist for both themes; the SVG renders identically on two consecutive builds (diff the two `dist/site-map/index.html` files: empty).

## Working rules (every E006 task)

- Repo root `C:/Users/zentala/code/zentala.eu`. Standards `DESIGN.md` and `src/AGENTS.md` are tracked and present in your worktree. Plan documents live under the main checkout's `.plan/` directory — read them by absolute path from `C:/Users/zentala/code/zentala.eu/.plan/`.
- First command in the worktree: `just setup`. Build: `just build`. Audit: `just audit`. Typecheck: `just typecheck`.
- Stage by explicit path (`git add <file>`), never `git add -A` / `git commit -a`; never stage the worktrees directory.
- Dev-only features are gated on `IS_PREVIEW` (`src/lib/preview.ts`), never on `import.meta.env.DEV`.
- Preview servers: start on a free port, kill BY PORT when done; never kill by process name.
- Colours, sizes, radii, fonts come from tokens; no hard-coded values in components.
- Both themes must render (`.dark` class on `<html>`); check light and dark.
- Stay inside your write set; another task in this wave owns the other files. A change needed elsewhere → one entry in the main checkout's `.plan/BACKLOG.md` with path:line.
