---
formatVersion: 1
type: feature
id: E006-T09
title: New content components from DESIGN.md
epic: E006
status: pending
priority: high
effort: large
agent: ts-dev
points: 8
model: standard
confidence: 4
summary: "Build the eleven content components the chapters will be rewritten with, exactly to their DESIGN.md contracts, plus a preview-only gallery page that renders them all in both themes."
created: 2026-09-25
wave: 3
completed_at: null
commits: []
verification_evidence: []
branch: feat/E006-T09
---

# E006-T09: New content components from DESIGN.md

Covers `DESIGN.md` Migration step 8 except `ChapterFooter` (E006-T11). Each component has a contract in the DESIGN.md Components section (props, slots, variants, tokens, do/don't) — implement the contract, not an interpretation. Where a contract names a token that does not exist yet (E006-T06 adds them in parallel), use the token name anyway with a fallback value inside `var()`.

## Write set (new files only)

- `src/components/Objection.astro`, `ObjectionList.astro`, `MeansForYou.astro`, `ComparisonTable.astro`, `Playbook.astro`, `KeyFigure.astro`, `SideNote.astro`, `ProvenanceNote.astro`, `PullQuote.astro`, `SourceList.astro`, `Figure.astro`
- `src/pages/ui/content.astro` — a second style-guide page (E004-T11 made `src/pages/ui/index.astro` the living style guide; E006-T07 owns that file this wave, so yours is a sibling page linked from nowhere yet), gated on `IS_PREVIEW`, rendering every component above with sample props in every variant
- `src/components/__tests__/` if you add tests

Do not edit existing components, layouts, styles or content (E006-T06/T07 own them this wave). `Surface.astro` (E004-T11, merge b424eb1) is the base surface with `hue` + `variant` — reuse it where a contract describes a boxed surface instead of re-implementing borders and backgrounds.

## Steps

1. Read DESIGN.md front matter (tokens), Components (the 11 contracts), Do's and Don'ts (24 rules).
2. Implement each component: semantic HTML, tokens only, both themes, none of the decoration the rules forbid (no hover-scale, gradients, shadows beyond the elevation tokens, coloured left borders unless the contract says so).
3. `ProvenanceNote` renders the five fields Source / Dictated / Added / Cut / Open; `ObjectionList` groups `Objection`s; `Figure` wraps any child with caption and source (the chart adoption is E006-T13).
4. `src/pages/ui/content.astro` gallery under `IS_PREVIEW`; in production the route must not exist.
5. `just build` (production) and `SITE_PREVIEW=true just build`; Playwright screenshots (`chromium.launch({ channel: 'chrome' })`) of `/ui/content/` in both themes to `.plan/reports/2026-09-25-e006/T09-light.png` and `T09-dark.png` in the main checkout; zero console errors.

## Acceptance

- `just build` exits 0 in both modes.
- All 11 component files exist and each declares typed `Props`.
- `dist/ui/content/index.html` does not exist after the production build and exists after the preview build.
- `grep -rn "#[0-9a-fA-F]\{3,6\}\b\|hover:scale\|shadow-lg\|gradient"` on the 11 files returns nothing.
- Two gallery screenshots exist; console clean.

## Working rules (every E006 task)

- Repo root `C:/Users/zentala/code/zentala.eu`. Standards `DESIGN.md` and `src/AGENTS.md` are tracked and present in your worktree. Plan documents live under the main checkout's `.plan/` directory — read them by absolute path from `C:/Users/zentala/code/zentala.eu/.plan/`.
- First command in the worktree: `just setup`. Build: `just build`. Audit: `just audit`. Typecheck: `just typecheck`.
- Stage by explicit path (`git add <file>`), never `git add -A` / `git commit -a`; never stage the worktrees directory.
- Dev-only features are gated on `IS_PREVIEW` (`src/lib/preview.ts`), never on `import.meta.env.DEV`.
- Preview servers: start on a free port, kill BY PORT when done; never kill by process name.
- Colours, sizes, radii, fonts come from tokens (`src/styles/_tokens.scss`, `tailwind.config.js`); no hard-coded values in components.
- Both themes must render (`.dark` class on `<html>`); check light and dark.
- Stay inside your write set; another task in this wave owns the other files. A change needed elsewhere → one entry in the main checkout's `.plan/BACKLOG.md` with path:line.
