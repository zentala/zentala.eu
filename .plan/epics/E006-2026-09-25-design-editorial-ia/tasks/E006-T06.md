---
formatVersion: 1
type: feature
id: E006-T06
title: Design tokens, ArticleGrid and article typography
epic: E006
status: pending
priority: high
effort: medium
agent: ts-dev
points: 5
model: standard
confidence: 5
summary: "Land the type and spacing tokens DESIGN.md proposes, build the named-track ArticleGrid, and move ArticleLayout and prose headings onto it so every long page shares one measure and one type scale."
created: 2026-09-25
wave: 3
completed_at: null
commits: []
verification_evidence: []
branch: feat/E006-T06
---

# E006-T06: Design tokens, ArticleGrid and article typography

Covers `DESIGN.md` Migration steps 1 and 2. Read the Typography, Layout and "Proposed additions" sections first; the token names and values are there, not here.

## Write set

- `src/styles/_tokens.scss`, `src/styles/global.scss`, `tailwind.config.js` (token exposure only)
- `src/components/ArticleGrid.astro` (new)
- `src/layouts/ArticleLayout.astro`, `src/layouts/Layout.astro`

Do NOT edit other components in this wave (E006-T07 and E006-T09 own `src/components/`). Replacing hard-coded values inside components happens in E006-T13.

## Steps

1. Tokens: add `--text-xs`, `--text-sm`, the `--leading-*` set, `--tracking-eyebrow`, `--font-mono`, `--space-xs … --space-section` with the values from the DESIGN.md front matter; expose them in `tailwind.config.js` where the existing tokens are exposed.
2. `ArticleGrid.astro` with named tracks per DESIGN.md Layout; `ArticleLayout.astro` adopts it (the six `max-w-*` prose widths and `font-light` go).
3. `.prose`: heading weights 700/400, H2 at `--text-3xl`; `h4`–`h6` Inter 600 (`Layout.astro:126` sets serif on all six today); prose links underlined.
4. `just build`; screenshot `/book/direct-democracy/` in both themes with Playwright (`chromium.launch({ channel: 'chrome' })`; bundled browsers are not installed) to `.plan/reports/2026-09-25-e006/T06-light.png` and `T06-dark.png` in the main checkout.

## Acceptance

- `just build` exits 0.
- `grep -c -- "--space-section\|--tracking-eyebrow\|--font-mono\|--text-xs" src/styles/_tokens.scss` ≥ 4.
- `src/components/ArticleGrid.astro` exists and is imported by `ArticleLayout.astro`.
- `grep -n "max-w-" src/layouts/ArticleLayout.astro` returns nothing.
- Two screenshots exist; the article measure is `--article-measure` in both.

## Working rules (every E006 task)

- Repo root `C:/Users/zentala/code/zentala.eu`. Standards `DESIGN.md` and `src/AGENTS.md` are tracked and present in your worktree. Plan documents live under the main checkout's `.plan/` directory — read them by absolute path from `C:/Users/zentala/code/zentala.eu/.plan/`.
- First command in the worktree: `just setup`. Build: `just build` (astro check + astro build + sitemap rename). Audit: `just audit`. Typecheck: `just typecheck`.
- Stage by explicit path (`git add <file>`), never `git add -A` / `git commit -a`; never stage the worktrees directory.
- Frontmatter `description` is 70–160 characters (schema-enforced).
- Dev-only features are gated on `IS_PREVIEW` (`src/lib/preview.ts`), never on `import.meta.env.DEV`.
- Preview servers: start on a free port, kill BY PORT when done; never kill by process name.
- Colours, sizes, radii, fonts come from tokens (`src/styles/_tokens.scss`, `tailwind.config.js`); no hard-coded values in components.
- Both themes must render (`.dark` class on `<html>`); check light and dark.
- Stay inside your write set; another task in this wave owns the other files. A change needed elsewhere → one entry in the main checkout's `.plan/BACKLOG.md` with path:line.
